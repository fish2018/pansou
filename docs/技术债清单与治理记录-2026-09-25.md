# 技术债清单与治理记录（2026-09-25）

承接 [TG 快速兜底搜索：性能分析、技术债与优化方案](./TG快速兜底搜索性能分析与优化-2026-09-24.md)。
本文记录该轮优化之后的债务清偿工作：消除了什么、依据是什么、哪些明确不做。

所有结论都来自本机实测，未实测的部分单独标注为"未验证"。

---

## 一、本轮消除的债

| # | 问题 | 处置 | 证据 |
|---|---|---|---|
| 1 | **插件路径是 TG 缓存污染的同源第二入口**：`searchPlugins` 同样无条件写 60 分钟 TTL，把一次抖动的残缺结果锁住整个缓存周期 | 抽出 `service/batch_search.go` 作为两条路径共用的完整度判定：失败与超时分开统计、TTL 按完整度分流、后台补齐 | 单测 11 个分支；插件路径端到端结果与改动前一致（739/745 → 736/743，属正常波动） |
| 2 | **上一轮我引入的过度收紧**：`TG_CHANNEL_TIMEOUT_SECONDS` 默认 3 秒，把聚合窗口压到单频道预算（4 秒）之下 | 默认值改为 `0` = 跟随单频道请求超时，即项目原有行为；只有实测确认不丢结果才收紧 | `searchChannelWithContext` 每请求自带 4 秒上限，批次自然在约 4 秒返回 |
| 3 | 123 处正则写在函数体内，每次调用重新解析模式 | AST 工具批量提取到包级（40 个文件）；同文件相同字面量共享一个变量 | 函数内残留归零；`ExtractPassword` 单次 23.5µs → 6.7µs |
| 4 | `plugin/dyyj` 两处按 pattern 字符串在循环里 `MustCompile` | 改为包级预编译切片 | 编译 + 单测通过 |
| 5 | `ParseSearchResults` 每条消息把正文序列化成 HTML、再正则换行、再剥标签、再解实体，而结果只是同一棵 DOM 的文本投影 | 新增 `messageTextWithBreaks` 直接从 DOM 拼出带换行文本 | **4 个真实页面 77 条消息，正文与标题输出零差异**；同批消息提取 458µs → 26.7µs（17×） |
| 6 | `nextPageParam` 声明后从未赋值，恒为空串（翻页功能从未实现） | 移除死变量，保留原签名兼容 | 残留引用 0 |
| 7 | 0 条结果无法区分"频道确实没有匹配内容"与"t.me 改版导致解析失效"，站点一改版就静默归零 | 新增 `PageParseStatus` 三态 + `ParseSearchResultsWithStatus` | 三态用例全通过，含真实无结果页结构 |
| 8 | 批次失败只汇总成一个数字，不知道是限流还是改版，也不知道谁拖慢了整批 | `httpStatusError` 类型化状态码 + `failureClass` 归类 + 慢项 Top5 | 日志直接定位出下面第四节的三项具体债务 |

### 第 7 项的一处自我纠正

"结构失效"最初用 `len(results) == 0` 判定，实测立刻误报了 5 个频道。核查后发现
`Lsp115`、`wpzyk`、`MCPH03` 都是**消息结构完好、只是整条不含受支持的网盘链接**
（页面里 0 个网盘链接、0 个 magnet），0 条结果是正确的。

改为用"识别成功的消息块数"判定，并补了误报回归用例。**这个误报是实测发现的，不是推理发现的。**

---

## 二、明确不做或暂缓的项

### 整页 DOM 换成手写分块扫描（暂缓）

实测整页解析 **14.8ms/页**（122–159KB、17–20 条消息），110 频道合计约 1.63s CPU。

但 pprof 显示解析路径的成本构成是：正则扫描 29.8%、`ExtractPassword` 24.0%、
DOM 构建 22.7%、cascadia 选择器 7.1%。分块扫描只能消掉 DOM 构建 + 选择器这约 30%，
**剩下的 54% 正则成本一分不动**。折算端到端约 2.4%（8 核）。

代价是 321 行语义复刻（标题分档、正文/键盘按钮链接合并、密码归属、去重规则），
回归风险与 2.4% 的收益不匹配。

**结论：不以"重构"名义重写这段。** 若将来单核部署导致 4 秒预算吃紧，再在有字段级等价测试的前提下做。

### 自动降级慢频道 / 慢插件（不做）

慢项只报告、不自动剔除。自动剔除会静默丢掉**仍在产出结果**的频道——
`yoyokuakeduanju` 耗时 3.7 秒但成功返回结果，剔除它就是拿数据完整性换延迟，
与"兜底数据要完整"的目标直接冲突。是否裁剪应由部署方按实测决定，因此只提供数据不下判断。

### 已被实测否掉的方案（不要重复尝试）

| 方案 | 实测结果 |
|---|---|
| 把 20 个域名合并成一个 alternation 正则 | 慢 2.7 倍 |
| 用 `strings.Contains` 做字面量前缀预筛 | 零收益 |
| 改用 `FindAllStringIndex` | 更慢、分配更多 |
| ETag / If-None-Match 条件请求 | 上游无 ETag/Last-Modified，结构上不可能 |
| 对冲请求（hedged request） | 方差≈0，无收益 |
| `antchfx/htmlquery` / `gumbo` | 分配 4 倍 / 已停止维护 |

---

## 三、新增配置项

全部默认值都**不改变项目原有行为**，只在需要时收紧或放开。

| 环境变量 | 默认 | 作用 |
|---|---|---|
| `TG_CHANNEL_TIMEOUT_SECONDS` | `0`（跟随单频道超时） | 频道批收集窗口 |
| `TG_CHANNEL_REQUEST_TIMEOUT_SECONDS` | `4` | 单个频道请求超时 |
| `TG_RESPONSE_MAX_BYTES` | `2097152` | 单频道响应体上限 |
| `TG_BACKFILL_ENABLED` | `true` | 超时频道后台补齐 |
| `CACHE_PARTIAL_TTL_MINUTES` | `3` | 结果不完整时的缓存有效期 |
| `PLUGIN_BATCH_TIMEOUT_SECONDS` | `0`（沿用 `PLUGIN_TIMEOUT`） | 插件批收集窗口 |
| `PLUGIN_BACKFILL_ENABLED` | `true` | 超时插件后台补齐 |
| `UPSTREAM_IDLE_CONN_TIMEOUT_SECONDS` | `600` | 上游空闲连接保活 |
| `UPSTREAM_MAX_IDLE_CONNS_PER_HOST` | `110` | 每主机空闲连接数 |

---

## 四、可观测性暴露出的具体债务（**均已定论：见各条"见第七节"标注；标题原写"尚未处置"已过时**）

上线失败分类与慢项统计后，第一次请求就暴露了下面三项。它们是**本轮发现、留给后续**的债：

| 对象 | 现象 | 影响 |
|---|---|---|
| `yingso` 插件 | 接口配置返回 `HTTP 404`，占当日失败的主要比例 | **已修复**，见第七节 |
| `u3c3` 插件 | 耗时 **12.5 秒**且失败 | **非插件缺陷**，见第七节 |
| `buerchen` 插件 | `站点要求安全验证 (CHALLENGE_REQUIRED, code=42901)，暂停请求 1 分钟` | 站点加了挑战，属外部变化 |
| TG 频道 `yoyokuakeduanju` | 耗时 **3.7 秒**但成功 | 决定频道批窗口长度的就是这一类少数频道 |

---

## 七、两项排查结论（含一次自我纠错）

### `yingso`：确认是插件失效，已修复

站点在 2026-09 变更了混淆接口，插件三处同时失效，因此长期只能报
"获取接口配置失败"。证据链：

| 环节 | 插件原值 | 线上现值 |
|---|---|---|
| 配置接口 | `GET /test` | `GET /test1`（旧路径 Express 404） |
| 配置 XOR 密钥 | `12345678` | `672134612` |
| 取分享链接 | `POST /{v}/getKey` | `POST /{v}/getKey670I23762183` |

旁证：`yingso.fun` 证书 `notAfter=2026-09-24 01:59:59 GMT`，已过期；
前端 bundle 的 request 模块显示搜索与取链接协议本身未变。

修复后实测：单插件 0.43 秒返回 30 条，链接有效；端到端插件成功数 52/72 → 54/72。
实现按"新端点优先、旧端点兜底"成对尝试，站点再次变更时不至于整体失效。

### `u3c3`：**不是插件缺陷**，是我的测试环境问题

日志里 `u3c3` 耗时 10–12 秒，一度被当作慢插件。核查后推翻：

- `u3c3.com` 在本机经代理访问返回 `SSL_ERROR_SYSCALL`
- `nslookup u3c3.com` 返回 `127.0.0.1`（DNS 查询被本地代理拦截）
- 系统解析器的结果却是真实 Cloudflare 地址 `2606:4700:3035::ac43:a04e`
- 同期对照组 `yingso.fun` 也变成 `000`

即**本机代理当时无法访问 `u3c3.com`**，与同批次的其它站点一起失败。插件的
镜像回退逻辑本身按设计工作（先试主镜像、失败后切兜底镜像并记住）。

结论：**不改插件镜像配置**。这与第二节"3 秒预算"是同一类错误——把本机环境的
特殊条件当成通用缺陷去改代码。两次都是实测复核推翻了初判。

---

## 八、插件 HTTP 客户端碎片化：先测量，再改一处

`plugin/` 下 77 个文件、104 处自建 `&http.Client{}`，**没有任何插件使用共享客户端**
`util.GetHTTPClient()`。其中 52 处显式设置 `Transport`，31 处在函数内每次新建。

排查中先纠正了一个自己的误判：`Transport` 为 nil 的 client 会用
`http.DefaultTransport`，**连接池仍是共享的**，所以"每次新建 client 就丢连接复用"
并不成立。真正的问题是 `DefaultTransport` 每主机只保留 **2** 条空闲连接。

同时发现插件框架自己给的客户端也是 `&http.Client{Timeout: ...}`（Transport 为 nil），
所以**之前为共享客户端加的调优（每主机 110、保活 600s）对插件路径完全没有作用**——
插件路径和 TG 频道路径走的是两套连接池。

### 测量（复现插件真实配置，5 轮 × 8 并发，同一主机）

| 配置 | 新建连接 | 每轮新建 |
|---|---|---|
| 调优前 `DefaultTransport`（idle/主机=2） | 32 条 | 6.4 |
| 调优后（idle/主机=20，保活 90s） | 8 条 | 1.6 |

即并发突发结束后多余连接不再被立刻关闭，下一轮搜索省掉重新握手。连接数下降 75%；
本机 TLS 握手便宜，耗时只差 8%（374.9ms → 364.0ms），真实网络每次握手 100-300ms。

**测量过程本身也修了一个错**：第一版基准没读 body 就 `Close`，连接无法复用，
导致"调优前后都是 8 条/轮"的假结论。Go 需要读到 EOF 才归还连接到空闲池。

### 实现：只改一处

在 `util.TuneDefaultTransport()` 中调优 `http.DefaultTransport` 的连接池，由
`InitHTTPClient()` 在 `config.Init()` 之后调用。这样框架客户端与 77 个文件里
Transport 为 nil 的自建客户端**全部**被覆盖，不需要动那 77 个文件。

（本节当时的"刻意不动 Proxy"结论是**错的**，已在第十一节更正——见下文。）

### 验证

- `util/http_util_test.go`：调优生效、配置覆盖生效
- 启动日志可观测：`[HTTP] 已调优 DefaultTransport：每主机空闲连接 110，保活 10m0s，代理=...`
- 端到端：yingso 单独搜索 1.97s / 1.29s（各 30 条），功能无回归

### 未做（有据）

- **不给 52 处自定义 `Transport` 的插件统一换连接池**：它们各有站点特性（TLS 指纹、
  超时、代理），统一替换需要逐个验证，收益重叠且风险高于收益。

---

## 五、验证方法

**隔离 A/B**：两个实例同时挂同一个上游代理会互相干扰。曾因此得到"优化后慢一倍"的
错误结论（12.1s vs 6.1s），隔离后两版耗时一致（`src=tg` 4.22/2.16s vs 4.33/2.17s，
`src=plugin` 13.30/13.93s vs 13.26/13.67s）。**并发环境下的对比数字一律不可信。**

**真实样本等价性**：改写解析逻辑时，用 4 个真实页面（`page_*.html`）复刻旧实现逐条比对，
77 条消息正文与标题零差异，而非只靠构造用例。

**契约测试**：`util/parser_contract_test.go` 锁定正文拼装、标题分档、解析三态；
`service/batch_search_test.go` 锁定缓存分流、补齐判定、失败归类、慢项排序。
标题用例走完整链路（HTML → DOM → 正文 → 标题），签名或实现替换都无法绕过。

---

## 六、回滚点

| tag | 内容 |
|---|---|
| `v2.1-tg-opt` | TG 快速兜底优化完成 |
| `v2.1-debt-1` | 本文件一～二节（批次 1-2） |
| `v2.1-debt-2` | 正文提取、解析三态、可观测性（批次 3-4） |
| `v2.1-debt-3` | yingso 接口变更修复、u3c3 归因（批次 5） |
| `v2.1-debt-4` | DefaultTransport 连接池调优（批次 6） |
| `v2.1-debt-5` | refresh 失败路径回退缓存（批次 7） |
| `v2.1-debt-6` | gying 关闭源处理（批次 8） |
| `v2.1-debt-7` | 代理解析统一与配置陷阱修复（批次 9） |
| `v2.1-debt-8` | 插件客户端超时改为调用时解析（批次 10） |
| `v2.1-debt-9` | 插件源全量审计 + 失败原因丢弃修复（批次 11） |
| `v2.1-debt-10` | 零产出盲区与后台补齐实证（批次 12） |
| `v2.1-debt-11` | 逐插件产出日志 + 49 处 Transport 补 Proxy（批次 13） |

回滚：`git reset --hard <tag>`

---

## 九、refresh 失败路径：插件结果凭空消失

### 现象

`refresh=true` 时 gying 结果消失。

### 根因（不是 refresh 机制本身）

refresh 机制是对的——它确实跳过了插件缓存。问题在失败路径上有两层吞错：

1. gying 的 `executeSearchTasks` 把每个用户的错误都吞掉，抓取失败时返回**空切片且不带 error**，
   插件以"成功、0 条"上报。而 refresh 恰好跳过缓存，两者叠加就表现为"一刷新结果就没了"。
2. 框架层 `AsyncSearch` 在 `err != nil` 时直接返回错误，调用方 `service.searchPlugins`
   在 `err != nil` 分支 `continue` **丢弃结果**。所以任何插件抓取失败都会从结果里消失，
   不只是 gying。

### 改动

- gying：`executeSearchTasks` 返回 `(results, error)`，区分"全部抓取失败"（`succeeded==0`
  且有错误）与"抓取成功但确实无匹配"——后者是合法空结果，不能当失败上报。
- gying：刷新或没有可用账号时，若已有缓存则回退缓存并打印日志；无缓存可回退时如实返回错误。
- 框架：`AsyncSearch` 与 `AsyncSearchWithResult` 的错误分支回退缓存，与超时路径
  `partial()` 的既有语义（"响应超时，返回部分缓存"）对齐。因调用方会丢弃 `err != nil`
  的结果，这里返回 `nil` error 并打印明确日志，兼顾数据可用性与可观测性。

### 验证

- `plugin/gying/gying_refresh_test.go`：刷新回退缓存 / 非刷新缓存命中 / 无缓存时不报错
- `plugin/plugin_fallback_test.go`：有缓存则回退、无缓存则如实报错
- 端到端：gying 的失败由"静默 0 条"变为可追溯的 `网络错误 x1`，附带 `Get "...": EOF` 原因

---

## 十、gying 站点已关闭：从默认启用列表移除

gying 站点已关闭（2026-09 确认）。它此前在 `ENABLED_PLUGINS` 默认列表中，
而站点关闭后该插件**每次搜索都必然失败**，且是插件路径的慢项（实测 6.0 秒），
白白占用一个插件槽位与批处理时间。

处理：

- `Dockerfile`、`docker-compose.yml` 的 `ENABLED_PLUGINS` 移除 `gying`
- `plugin/gying/gying_live_test.go` 无条件跳过并注明原因，主体代码保留供站点恢复后复验

注意 `EnabledPlugins` 的语义是"空表示启用所有，非空即白名单"，因此这次移除是**可逆**的：
需要时把 `gying` 加回环境变量即可，插件代码未做任何删改。

同时说明：gying 的可用性此前只是"本机代理不通"，无法据此判断站点状态；本次是按明确
信息（站点关闭）处理，而不是根据本机网络推断——避免重复第二节"3 秒预算"那类
把环境特例当通用缺陷的错误。

---

## 十一、代理解析统一（含对第八节结论的更正）

### 先更正一个错误结论

第八节曾写："共享客户端的 `applyProxy("")` 什么都不做，**它只认 `config.ProxyURL`，
不认 `HTTP_PROXY` 环境变量**，所以不能拿给插件用"。**这个结论是错的。**

`config.getProxyURL()`（config.go:220）本身就是多来源解析：

```
PROXY → HTTPS_PROXY/https_proxy → HTTP_PROXY/http_proxy → ALL_PROXY/all_proxy
```

也就是说 `config.ProxyURL` **已经合并了这些环境变量**，共享客户端一直是认环境变量的。
当时只看到 `applyProxy("")` 提前 return，就跳过了"config 已经把环境变量填进来"这一步，
把一个不存在的阻塞当成了结论。顺带纠正参数名：环境变量是 **`PROXY`**（README 第 99 行），
`ProxyURL` 只是 Go 结构体字段名，不是可配置的参数名。

### 真正的缺口在反方向

核实 `httpproxy`（`ProxyFromEnvironment` 的实现）后确认，它只读
`HTTP_PROXY` / `HTTPS_PROXY` / `NO_PROXY`，**不读 `PROXY`，也不读 `ALL_PROXY`**：

| 变量 | 共享客户端（TG 频道路径） | `ProxyFromEnvironment`（插件路径） |
|---|---|---|
| `PROXY` | ✅ 读（README 主推参数） | ❌ **不读** |
| `HTTPS_PROXY` / `HTTP_PROXY` | ✅ 读 | ✅ 读 |
| `ALL_PROXY` | ✅ 读 | ❌ **不读** |
| `NO_PROXY` | ❌ 不处理 | ✅ 处理 |

插件路径全部走 `http.DefaultTransport`，因此**只配置 `PROXY`（或只配置 `ALL_PROXY`）时，
所有插件都会静默退回直连**——而 TG 频道照常走代理。这是个真实的配置陷阱：
按 README 最推荐的 `PROXY=...` 配置，恰好是插件失效的那种配法。

### 改动

1. 新增 `config.NoProxy`（读取 `NO_PROXY`/`no_proxy`），沿用项目"环境变量集中在 config"的约定。
2. `util.BuildProxyFunc(rawProxyURL, noProxy)` 统一代理解析：显式配置的代理优先并
   **按 `NO_PROXY` 放行直连**（`http.ProxyURL` 不做这件事，改用 `x/net/http/httpproxy` 的
   `Config.ProxyFunc()`）；未配置任何代理时交回 `ProxyFromEnvironment`，保持标准语义。
3. `TuneDefaultTransport()` 一并接线代理，插件路径与频道路径从此同一套解析。
4. 顺带去掉 `x/net/proxy` 的 socks5 自定义 `DialContext`：`Transport.Proxy` 原生支持
   `socks5`（含账号密码、本地解析域名），`socks5h` 归一化为 `socks5`。这样 TCP 拨号
   参数不再被代理分支覆盖。

### 验证（对照组自证）

`util/http_util_test.go` 新增 7 项用例。其中 `TestPluginPathProxyWiringLive` 用
`google.com` 作判别（本机直连不通、经代理可通），且**自带对照组**：

- 对照组（把 `Proxy` 还原为修复前的 `ProxyFromEnvironment`，只设 `PROXY`）→
  `context deadline exceeded`，确认修复前确实退回直连
- 修复后 → `200`

对照组失败才继续断言，若本机直连可达则直接 skip，避免用例失去判别力还装作通过。

---

## 十二、插件客户端超时被构造阶段冻结（配置静默失效）

### 问题

插件是在**各自包的 `init()`** 里构造的（`RegisterGlobalPlugin(NewYingsoPlugin())`），
而 `config.Init()` 在 `main.go:120` 的 `initApp()` 里才执行。也就是说构造那一刻
`config.AppConfig` 恒为 `nil`，构造函数里 `if config.AppConfig != nil` 的分支永远走不到。

后果有三个配置项被静默忽略：

| 配置项 | 期望 | 实际（修复前） |
|---|---|---|
| `ASYNC_RESPONSE_TIMEOUT` | 决定短超时客户端 | 恒为硬编码 4s |
| `PLUGIN_TIMEOUT` | 决定长超时客户端 | 恒为硬编码 30s |
| `ASYNC_CACHE_TTL_HOURS` | 决定内存缓存有效期 | 恒为硬编码 1h |

注意 `AsyncSearch` 里的 **select 超时**是在请求时读配置的，所以配小值（如 1s）看起来
"生效"；但配大值（如 12s）时，select 会等 12s，而底层客户端早在 4s 就报错——表现为
"超时配置调大了却没有任何效果"。Dockerfile 恰好把 `ASYNC_RESPONSE_TIMEOUT` 设为 4
（等于硬编码默认值），所以默认部署完全看不出来。

### 改动

超时与缓存有效期改为**调用时解析**，不再在构造阶段读取：

- 新增 `pluginClientTimeouts()` 与 `(*BaseAsyncPlugin).getCacheTTL()`
- `client` / `backgroundClient` 改为懒创建（各自一个 `sync.Once`），首次取用时按
  当时的配置构造；`GetClient()` 的对外签名不变
- `p.cacheTTL` 的 9 处读取改为 `p.getCacheTTL()`
- 首次解析时打一行 `[PLUGIN] 插件客户端超时按当前配置解析：响应 X，处理 Y`，
  让"配置到底生不生效"可以被直接看到，而不是靠猜

### 验证

- `plugin/plugin_config_lazy_test.go`：先构造、后配置 → 取到配置值；无配置 → 退回
  硬编码默认值；重复取用返回同一实例（不丢连接复用）
- 运行时：`ASYNC_RESPONSE_TIMEOUT=11 PLUGIN_TIMEOUT=45` 启动，日志输出
  `响应 11s，处理 45s`，搜索 30 条无回归

---

## 十三、插件源全量审计（75 个）与失败原因可观测性

### 方法与判别原则

新增门控审计 `main_plugin_audit_test.go`（默认跳过，`AUDIT_PLUGIN_SOURCES=1` 才跑）：
逐个调用已注册插件并按 `refresh` 取一轮，等 35 秒让后台补齐完成后再复核一轮，
两轮都没结果才算"空"。

判别原则（这条比结论本身重要）：

- **只有"解析/结构类"错误**才能断定插件自身失效——这类证据与本机网络无关。
- **403 / 444 / EOF / connection reset 一律不可判定**：它们高度可能是代理出口 IP
  被拒，本机环境不足以区分"站点封了我的出口"与"插件坏了"。
- 任何结论都不从本机网络推广到部署环境（`u3c3`、`gying` 两次教训）。

### 结果

75 个插件：健康 29、抓取失败 20、两轮后仍空 26。

**可判定为站点/插件层面问题（环境无关）**

| 插件 | 现象 | 判断 |
|---|---|---|
| `cldi` | `重试 3 次后仍然失败: %!w(<nil>)` | **代码缺陷**，已修，见下 |
| `clxiong` | 期望 302 重定向，实得 400 | 站点协议变更 |
| `xb6v` | 未找到搜索结果页重定向信息 | 页面结构变更 |
| `erxiao` | `lookup www.2xiaopan.top: no such host` | 域名已不存在 |
| `sousou` | `page 3, type BDY: 567` | 第 3 页失败，前两页可用 |
| `buerchen` | `CHALLENGE_REQUIRED code=42901` | 站点加挑战（先前已记录） |

**不可判定（需在真实部署环境复核，不能据此裁剪）**

- 403：`dyyj` `dyyjpro` `libvio` `miosou` `quarktv` `zlxapp`
- 444：`dy4k` `yunsou`
- EOF：`hunhepan` `leso` `rrbt` `u3c3` `xdpan`
- connection reset：`thepiratebay`

**两轮后仍空的 26 个**里，有一组耗时精确落在 **4.000s / 4.004s / 4.027s**：
`duanjuw` `duoduo` `feikuai` `kpkuang` `pan365` `quark4k` `quarksoo` `sopanya`
`zhizhen`。这正好是 `ASYNC_RESPONSE_TIMEOUT` 的默认值，说明它们是**卡在响应超时
返回了部分空结果**，而不是"真没有匹配内容"。这一组值得单独跟进，但性质是"慢"，
不是"死"。

### 已修：失败原因被丢弃（41 个文件）

审计里 `cldi` 报出 `%!w(<nil>)`，顺着查到一段被复制到 41 个插件的重试循环：

```go
resp, err := client.Do(reqClone)
if err == nil && resp.StatusCode == 200 {
    return resp, nil
}
if resp != nil {
    resp.Body.Close()
}
lastErr = err        // err == nil 时把 lastErr 清空
```

服务器返回非 200 时 `client.Do` 的 `err` 为 `nil`，于是 `lastErr` 被置空，重试三次后
只能报 `%!w(<nil>)` 或干脆只剩"重试 N 次后仍然失败"，**真实状态码彻底丢失**。
这与前几批"让失败可观测"是同一条主线：没有状态码就无法区分反爬、改版与网络抖动。

改动：`err == nil && resp != nil` 时补一个 `err = fmt.Errorf("HTTP 状态码 %d", status)`，
只在原处加信息，不改重试与退避逻辑。41 个文件统一处理，另 5 个经人工核对**不是同一
问题**（`kpkuang`/`hjzhencai`/`miosou` 的赋值在 `err != nil` 分支内；`clmao`/`wuji`
是"不看状态码就返回"，属行为差异，不在此次范围）。

验证：修复后 `cldi` 的报错变为
`[cldi] 搜索第一页失败: ... 重试 3 次后仍然失败: HTTP 状态码 4xx`。

### 测量后记录、未改动

- **19 处插件手写 `Transport: &http.Transport{...}`，没有一处设置 `Proxy`**：
  这些插件完全无视代理配置（包括刚统一的 `PROXY`）。改动需要逐个保留其
  TLS/超时特性，且审计显示这些插件的真实问题在别处，故记录不批量改。
  将来要动，正确做法是 `http.DefaultTransport.(*http.Transport).Clone()`
  再覆盖各自的自定义字段（继承代理与连接池）。
- **`qqpd` 在二维码登录路径内联了 4 个 `&http.Transport{}`**：既丢连接池
  （零值 Transport 退回默认每主机 2 条）也没有代理，且带 `InsecureSkipVerify`。
  属登录链路，非搜索热路径。
- **"52 处自定义 Transport"的旧说法要修正**：真正"每次调用新建独立连接池"的
  只有 `qqpd` 的 4 处（`panyq` 经核对是复用 `client.Transport`，误报）；
  其余 48 处只在构造时建一次，属正常。

---

## 十四、慢插件是"慢"不是"死"（实证），以及"成功但零产出"的盲区

### 先做实验，再决定要不要改

第十三节里那一组耗时精确落在 4.000s 的插件（`duanjuw` `duoduo` `feikuai`
`kpkuang` `pan365` `quark4k` `quarksoo` `sopanya` `zhizhen`），先用同一批插件、
同一个关键词对比不同预算：

| `ASYNC_RESPONSE_TIMEOUT` | 响应耗时 | 返回条数 |
|---|---|---|
| 4（默认） | 4.01s | **0** |
| 15 | 13.7s | **55** |

**结论：它们是慢，不是死。** 给足预算就有 55 条，默认窗口下一条都拿不到。

紧接着验证后台补齐是否真的把这些结果送达：4 秒窗口下第一次搜索返回 0 条，
等 25 秒后再搜（不刷新）——**0.005s 命中缓存，55 条**。

也就是说设计意图是成立的：快路径按 4 秒返回，慢插件的内容随后由后台补齐进入缓存。
**这 9 个插件不需要改动。**

### 但暴露了一个真盲区：`complete()` 不看出没出结果

同一批日志里写着"慢项 `quark4k` 耗时 4000ms **成功**"，汇总写着"成功 7/9"，
而用户这一轮实际拿到 0 条。两类完全不同的情况被同一个"成功"混在一起。

更严重的是缓存判定。原 `complete()` 只要求"没有超时也没有失败"：

```go
func (o *batchSearchOutcome) complete() bool { return len(o.missing) == 0 && o.failed == 0 }
```

于是"全部成功但零产出"会被判为**完整结果**，按完整 TTL（默认 60 分钟）缓存一整个
周期；而 `shouldBackfill` 又因 `timedOut() == 0` 不触发补齐。空结果就这样被锁住一小时。

这与第一批消掉的债（"插件路径是 TG 缓存污染的同源第二入口"）是同一类问题，
只是触发条件从"失败"换成了"成功但没内容"。

### 改动

- 新增 `observeYield(id, contributed)`：记录每个任务本轮真正贡献的可用结果条数
- 新增 `requireYieldTracking()`：**只由插件路径开启**。插件路径下 4 秒窗口返回空是
  常态；频道路径不开启——频道确实可能没有匹配内容，那种空结果应当照常缓存。
  若把频道路径也纳入，会让"无匹配"的正常批次每轮都重新抓取一遍。
- `cacheTTL`：插件路径下整批零产出直接不写缓存
- `logSummary`：新增"本轮零产出 N"计数与明细名单，并让它单独也能触发输出
  （原来只有失败或超时才输出，全空批次反而一声不响）

### 验证

- `service/batch_search_test.go` 新增 4 个子用例：插件路径整批零产出不写缓存、
  有任意产出即恢复正常判定、频道路径零产出仍照常缓存、零产出不改变失败/超时判定
- 实测冷启动（全新缓存目录）：第一次搜索 4.01s / 0 条，日志输出

```
[searchPlugins] 仙逆：成功 8/9，失败 1，超时未完成 0，本轮零产出 8；失败原因 网络错误×1（示例: ...）
[searchPlugins] 仙逆：本轮零产出（内容需靠后台补齐）: pan365 quark4k feikuai duoduo duanjuw zhizhen kpkuang quarksoo
```

修复前这一行只有"成功 8/9"，完全看不出用户其实一条都没拿到。

---

## 十五、逐插件搜索结果日志：框架此前在正常路径一行都不打

### 现象与根因

"几十个插件同时搜索时只有少数插件有日志"不是错觉，也不是日志被吞。核实后是三个叠加原因：

1. **框架只在异常路径打日志**。`plugin/plugin.go` 的全部输出点都挂在异常分支上：
   响应超时返回部分缓存、抓取失败回退缓存、缓存更新失败、后台刷新完成。
   一条**正常成功**的插件搜索，框架一行都不输出。
2. **插件自身大多不打日志**。75 个插件里只有 19 个文件的搜索路径含 `fmt.Printf`；
   定义 `DebugLog` 开关的只有 9 个，且**全部为 `false`**。
3. 因此能被看到的，只是少数自己无条件打日志的插件。

### 改动

- `logYields`：**始终输出**一行逐项产出，覆盖每一个真正返回过的任务（含失败的），
  形如 `插件产出 6 个 -> yingso=30 sopanya=0 pan365=0 duoduo=0 quark4k=0 kpkuang=0`。
  它回答的是"某个插件到底跑没跑、跑出几条"，与完整度无关，因此不受
  "仅在有失败时才输出"的限制。
- 失败项也进逐项记录（`=失败`），否则这一行会漏掉失败的插件，看日志的人无从确认它跑没跑。
- 新增可选开关 `PLUGIN_SEARCH_DETAIL_LOG`（**默认关闭**）：打开后每个插件打一行，
  带耗时与失败分类，供逐项排查。默认关是因为几十个插件每次搜索各打一行会把日志冲淡。
- 只作用于插件路径：频道路径 110 个频道逐项列出没有价值，故不启用。

### 验证

- 实测 6 个插件：`插件产出 6 个 -> yingso=30 sopanya=0 pan365=0 duoduo=0 quark4k=0 kpkuang=0`
- 实测含失败的组合：`插件产出 4 个 -> buerchen=失败 yingso=30 erxiao=失败 libvio=失败`

---

## 十六、手写 Transport 一律缺 Proxy（49 处，已补 + 守卫）

### 结论修正

第十三节说"19 处手写 Transport"，那是 `Transport: &http.Transport{` 这一种写法的计数。
按块扫描全部 `&http.Transport{`（含赋给变量的），实际是 **49 处**，分布在 46 个文件。

而且它们**并不业余**：绝大多数显式设了 `MaxIdleConns` / `MaxIdleConnsPerHost` /
`MaxConnsPerHost` / `IdleConnTimeout`。唯一缺的是 `Proxy` 字段——零值含义是
"永远直连"，于是**部署方配了代理，这些插件全部静默失败**，而 `DefaultTransport`
已被 `TuneDefaultTransport` 接好代理，两条路径行为不一致。

### 改动

- 新增 `util.ProxyFuncForTransport()`，按项目统一规则解析（`PROXY` → `HTTPS_PROXY` →
  `HTTP_PROXY` → `ALL_PROXY`，并按 `NO_PROXY` 放行直连），供插件手写 Transport 时赋值。
- 49 处统一补上 `Proxy: util.ProxyFuncForTransport(),`，其中 45 个文件补了 `pansou/util` 导入。
- 新增**防回归守卫测试** `plugin/plugin_transport_proxy_test.go`：直接扫描源码，
  任何手写 Transport 缺少 `Proxy` 就失败。已做对照验证——删掉 `zxzj` 的一处后
  测试精确报出 `zxzj/zxzj.go:38`。

### 验证

- 全部 49 处扫描通过；`util/http_util_test.go` 新增用例确认该助手按配置解析代理并遵守 `NO_PROXY`

---

## 十七、两处自我纠正（都被本人实验推翻）

### `erxiao`：域名**并未**消失

第十三节把 `lookup www.2xiaopan.top: no such host` 判为"域名已不存在"。用公共 DNS
交叉验证后推翻：`nslookup www.2xiaopan.top 8.8.8.8` 正常返回
`xiaomi666.cf-site.1o.ee` → `104.18.42.163`（Cloudflare）。

即**本机解析失败是环境问题，不是域名消失**。该插件退回"不可判定"。这是第三次
同类教训（前两次是 `u3c3` 与 `gying`）：本机网络不足以判断源站死活。

### `clxiong`："重试复用请求体"假设被对照实验推翻

审计里 `clxiong` 报"期望302重定向，但得到状态码: 400"。我一度认定根因是重试循环
复用同一个 `req`、body 已被消费。**做了对照实验后推翻**：把代码改回 `client.Do(req)`
（即所谓"有 bug"的写法），测试服务器三次收到的请求体**都是完整的**。

原因是现代 Go 的 `Transport.roundTrip` 会用 `GetBody` 把请求体回卷
（`strings.NewReader` / `bytes.Reader` 这类由 `NewRequest` 自动填好 `GetBody`），
所以复用请求在重试里是安全的。改动已全部撤销（`git checkout` 还原 + 删除测试），
不留"修了个不存在的问题"的代码。把这个反例记下来，避免以后再"修"一次。

### `clxiong` / `xb6v` 的实际情况

站点探测（含代理）：

| 站点 | 直连 | 经代理 | 插件现象 | 判断 |
|---|---|---|---|---|
| `www.cilixiong.org`（clxiong） | 200 | 200 | 期望 302，实得 400 | 站点搜索**行为变更**：直接返回一个"信息提示"页（3 秒后 `history.go(-1)`），不再是重定向 |
| `www.66ss.org`（xb6v） | 000 | **200** | 未找到搜索结果页重定向信息 | 站点**可达**，插件预期与现状不符 |
| `www.2xiaopan.top`（erxiao） | 000 | 000 | DNS 解析失败 | **不可判定**（公共 DNS 正常） |

两个站点都没有失效，是插件实现落后于站点现状；按新流程适配需要先摸清站点当前
的搜索交互，属独立任务，不在本次范围。

---

## 十八、全项目技术债梳理（结论：**未清完**）

本节是第一次**系统性地**扫全仓，而不是跟着故障报告走。方法：按主题拆成 5 个独立方向
并行审计（配置一致性、并发与生命周期、缓存正确性、错误处理与资源泄漏、输入处理与鲁棒性），
每条结论都带 `file:line`，且要求审计员区分"已证实"与"存疑"。

复核：凡标【已复核】的，是我自己打开代码再次确认过的。

### 高：构造时序类（与已修的两处同一类，仍有一处）

| # | 问题 | 位置 | 状态 |
|---|---|---|---|
| 1 | `ASYNC_MAX_BACKGROUND_WORKERS` 是死配置：`initAsyncPlugin()` 被插件构造函数在包 `init()` 阶段触发，容量定死为硬编码 20 且 `initialized=true` 让 `main.go:149` 的补正直接返回 | `plugin/plugin.go:344-362`、`:477` | **已修（`v2.1-debt-13`）** |
| 2 | `Config.UseProxy`、`Config.PluginTimeoutSeconds` 两个字段全仓零消费者 | `config/config.go:19/35` | 待处置【已复核：grep 仅定义与赋值】 |
| 3 | `SHARD_COUNT` 只活在 `README.md:169`，分片数实际由 CPU 核数算 | `util/cache/sharded_memory_cache.go:54-64` | 待处置（文档承诺不存在） |

### 高：缓存正确性

| # | 问题 | 位置 | 说明 |
|---|---|---|---|
| 4 | **插件缓存键漏掉 ext**：两个 `ext` 不同、结果集也不同的请求共用一条缓存 | `plugin/plugin.go:610/893`、`util/cache/cache_key.go:68-78` | 【已复核】`ext` 确实影响结果（`sdso` 的 `pages_per_type`、`cyg` 的 `per_page`）。例：同关键词同插件，一个是默认、一个带 `ext.pages=5` → 后者直接吃前者的结果 |
| 5 | **列表哈希无分隔符**：`["ab","c"]` 与 `["a","bc"]` 哈希相同 | `util/cache/cache_key.go:215-221` | 【已复核】逐项 `h.Write` 无边界 |
| 6 | 拼接无转义：`["a","b"]` 与 `["a,b"]` 的 `channelsHash` 都是 `a,b` | `cache_key.go:127/188/235/246` | POST body 可构造含逗号元素 |
| 7 | **TTL 分流被磁盘→内存回填绕过**：磁盘命中后固定用完整 TTL 写回内存，3 分钟的 partial 结果被续成 60 分钟 | `util/cache/enhanced_two_level_cache.go:104-110` | 直接废掉前几批建立的"部分结果短 TTL"机制；service 读缓存不校验新鲜度（`search_service.go:1269-1270` 注释明写） |
| 8 | `channelHashCache` / `pluginHashCache` 只增不删，键来自用户请求参数 | `cache_key.go:137-145/202-210` | 无界增长 |

### 高：并发

| # | 问题 | 位置 | 说明 |
|---|---|---|---|
| 9 | **共享插件单例上的 `MainCacheKey`/`currentKeyword` 被每请求覆写** → 并发不同关键词时结果可能写进别人的缓存槽 | 写：`service/search_service.go:1512-1513/1652`；读：`plugin/plugin.go:1043/1112/1227` 与 106 处插件调用 | 【已复核】`plugin/plugin.go:64-69` 的注释已经为 context 修过同一隐患（"放在 ext 里传递…避免并发请求共享同一个插件实例时互相覆盖"），却把这两个字段留在了实例上 |
| 10 | TG 频道路径并发无上限：`maxWorkers = len(channels)`，而 `channels` 由请求参数决定且只判空 | `service/search_service.go:1300`、`api/handler.go:37-48/172-174` | `concurrency` 参数只作用于插件路径 |
| 11 | `DelayedBatchWriteManager` 跨锁域竞争：`m.stats.*` 混用 atomic 与裸写，且不持 `queueMutex` | `util/cache/delayed_batch_write_manager.go:437-452` vs `:822-845` | 另有调优 goroutine 写 `config.MaxBatch*`、批处理 goroutine 读同字段 |

### 中（择要）

- `xb6v` 是唯一彻底无超时的网络路径（`Timeout=0` + 无 ctx）→ goroutine 会滞留在读响应上（`plugin/xb6v/xb6v.go:106-129/174-178`）
- 8 处插件把信号量获取写在 goroutine 内部 → goroutine 总数 = 上游返回条数（`diduan` `dyyj` `susu` `cyg` `zxzj` `gying` `rrbt` `pansearch`）；`panyq` 对解析出的候选 ID 完全无限制
- `finalUpdateTracker` 只增不删；且先 mark 后调 updater，失败即永久跳过（`plugin/plugin.go:1191-1226`）
- 同关键词多插件并发更新主缓存是"读-改-写非原子"，后写覆盖先写 → 丢结果（`search_service.go:254-321`）
- 插件级 `apiResponseCache` 的清理只挂在"缓存命中"路径 → miss 多的流量下无界增长（`plugin/plugin.go:432`）
- 磁盘缓存：驱逐后仍无条件写入、`shardSize` 下限 1MB 使小配置实际超容 1.6~3.2 倍、无文件数上限、非原子写、每次命中写 `.meta`
- 写缓存相关的 goroutine 全无并发上限或背压
- 过期插件数据被当本轮结果写主缓存并续期（`plugin/plugin.go:613-627/902-918`）

### 中：配置与文档

- 布尔解析分两派且语义不对称：取反式 `!= "false" && != "0"`（`config.go:272/365/433/477/499`）vs 正向式 `== "true" || == "1"`（`:442/720`）。`ENABLE_COMPRESSION=TRUE` 与 `CACHE_ENABLED=TRUE` 结果相反【已复核】
- 配置解析失败全程静默（`config` 包内零日志），所有 `Atoi`/`ParseBool` 失败直回默认值
- 环境变量绕过 config 层 11 处（`main.go` 6 处、`delayed_batch_write_manager.go` 5 处），`CACHE_PATH` 语义分裂（config 用绝对路径、插件用相对路径）
- `CHANNELS` 默认值三套：`README.md:101`=`tgsearchers3`、`:204`=`tgsearchers4`、`config.go:146`=`tgsearchers6`
- `Dockerfile:61-62` / `docker-compose.yml:21-22` 硬编码 20/100，两个 README 却描述为自动计算
- `config/config.go:39` 注释"空表示启用所有"与实现相反（实现是空=零插件）

### 测试与可维护性

- 覆盖率（**本节原值已过时，以下是 2026-09-25 之后的实测值**）：`util/cache` 33.6%、`api` 7.4%、
  `config` 1.3%、`util/json` 36.4%、`service` 14.9%、`plugin` 37.7%、`util` 53.9%、`util/pool` 70.0%。
  原先多个包为 **0.0%**，现已不再为 0；`model` 仍接近 0（纯数据结构，优先级最低）。
- 重试实现：**本条已收口**。通用重试 28 处已收敛到 `util.DoWithRetry`（`DelayFunc` 表达各种退避曲线、
  `WaitFunc` 表达可取消等待、`Abort` 表达"不该重试"）。剩余 6 处经逐个定性**不是通用重试**：
  gying/miosouL143/pansearch 循环体内无请求，miosouL95 是 Anubis 门禁握手，ting77 是重定向回调。
  41 个文件的 `%!w(<nil>)` 亦已修完。原写"33 个复制粘贴模板"是把不同性质的东西算在了一起。
- 最长函数 `ParseSearchResultsWithStatus` 455 行（`util/parser_util.go:168`）；最大文件 `plugin/gying/gying.go` 3562 行
- 两条搜索路径仍各写一遍收尾逻辑（`searchTG`+`backfillTGChannels` ~156 行 vs `searchPlugins`+`backfillPlugins` ~279 行）
- `go vet ./...` 现已全通过（`panlian` 的重复 json tag 已修）；全仓 `TODO/FIXME` 为 0；无未被引用的导出函数

### 仍在跑

**已回收（原写"尚未回收"已过时）**：错误处理与资源泄漏、输入处理与鲁棒性两个方向均已审计并处置，
见下节与"上游响应体读取封顶""解压炸弹防护""吞错处置"等章节。

### 补充：错误处理与资源泄漏方向（审计已回收）

| # | 问题 | 位置 | 状态 |
|---|---|---|---|
| 12 | **可触发进程崩溃的越界**：`l.title`/`l.d`/`l.i` 是上游三个互相独立的 JSON 数组，搜索循环边界只看 `len(I)`、旧守卫只校验 `len(Title)`，而 `L.D[index]` 无任何守卫；goroutine 内越界且全仓 171 个 goroutine 都无 `recover` → 进程退出而非单次请求失败 | `plugin/gying/gying.go:2675/2818` | **已修**（对照实验：旧守卫下 `panic: index out of range [0] with length 0`） |

已证实但未处置：

- **72 处 `io.ReadAll(resp.Body)` 无 `io.LimitReader`**（49 个文件，其中 70 处所在文件通篇没有 `LimitReader`）。读取量完全由远端决定，且这些请求本来就大量并发。同仓已有正确写法可对照（`miosou.go:166`、`5266ys.go:188`、`xiaokupan.go:189`、`qqpd/api.go:84`、`sousou.go:362`）——说明是遗漏而非有意。
- **5 处 `defer resp.Body.Close()` 写在重试循环体内**，`continue` 路径不释放（`bixin.go:213`、`pan666.go:213`、`quark4k.go:214`、`quarksoo.go:109`、`lingjisp.go:255`）。
- **26 处吞掉错误**：23 处 `_ =` + 3 处 `X, _ :=`。值得注意的几处：`enhanced_two_level_cache.go:58` 磁盘写失败无痕迹；`panlian.go:671/681/1428/1490/1493` Cookie/登录态持久化失败被丢弃；`check_service.go:270/340/392/498/530` 解析失败后继续用零值结构体判定；`check_service.go:1206-1276` bolt 事务错误全部丢弃；`nsgame.go:368` 读体失败会返回「半截 data + nil error」当成功。
- `huban.go:423`、`erxiao.go:373`、`panta.go:343/365/385` 用单值类型断言（无 `, ok`）且位于无 recover 的 goroutine 内。同仓对应位置都写了 `, ok`。当前各 sync.Map 只有一处同类型 `Store`，**构造不出失败输入**，故列隐患而非已证实缺陷。

反向结论（审计确认的「没有」）：响应体未关闭 0 处、`%w` 包 nil 0 处、测试文件无泄漏。

仍在跑：输入处理与鲁棒性方向。

### 补充：输入处理与鲁棒性方向（审计已回收）

| # | 问题 | 位置 | 状态 |
|---|---|---|---|
| 13 | **可触发进程退出的越界**：`difficultyBits` 直接来自第三方响应 JSON（`nsgame.go:67-70`）且无上下界校验，`fullBytes = bits/8` 用来索引 `sha256.Sum256` 的 `[32]byte`；≥264 时 `sum[i]` 立即越界，该函数跑在无 recover 的 goroutine 里（`plugin.go:950`） | `plugin/nsgame/nsgame.go:378/381/386` | **已修**（对照实验：去掉校验即 FAIL） |

审计给出的精确可达性（比"可触发"更有用）：

- `difficultyBits >= 264` → `sum[i]` 在 `i=32` 处**立即**越界，真实可达。
- `257..263` → 要走 `sum[fullBytes]`，需前 32 字节**全为零**（2^-256），实际不可达；但旧代码在这个区间会白跑 1000 万次循环（实测 14.9 秒）才返回失败，守卫顺带修掉了这个。
- 负数 → `fullBytes` 为负、循环不执行，会**静默返回错误 nonce**，一并挡掉。

已证实但未处置（用户可直接触发的资源放大）：

- **`channels` 无数量上限**：`api/handler.go:37-48` 只做 Split+TrimSpace，而 `search_service.go:1300` 用它当 `maxWorkers` → 传 10 万项即 10 万 goroutine + 10 万出站请求（与并发审计的高 10 同一条，两路独立确认）。
- **`/api/check/links` 三项叠加**：`items` 无数量上限（`check_handler.go:32-35` 仅拦空）、`proxy_url` 由请求体指定（`model/check.go:12-13` → `http_util.go:180-199` 只做语法校验 → 连向任意 host:port，含内网，**SSRF 形态**）、响应体与 gzip 解压均无上限（`check_service.go:1012/1388/1397`，解压炸弹）。
- **POST /api/search 请求体无上限**：`api/handler.go:159` 的 `c.GetRawData()` 在 gin v1.9.1 里就是 `io.ReadAll(c.Request.Body)`，无 `MaxBytesReader`；且**认证默认关闭**（`config.go:718-721` 读 `AUTH_ENABLED`，未设即 false）。
- TLS 校验跳过 5 处且**均无注释**：`panyq.go:105`（搜索路径生效）、`qqpd.go:1551/1664/1775/1862`。对照 `http_util.go:113` 是 `false`。
- `main.go:268/272/276` 把完整代理地址（含 `user:pass@`）打进日志，而同仓 `http_util.go:88-94` 专门写了脱敏函数却没被调用。
- `util/http_util.go:287` 的 `channel` 未转义拼进 `https://t.me/s/`（同函数 289 行的 keyword 是转义了的），可污染路径与查询串。
- `util/compression.go:50-58` GzipMiddleware 全量缓冲并双写整份响应。

审计**复核排除**的 4 组（子代理上报后被证伪，值得记录以免重复怀疑）：`ypfxw`/`yiove` 的 substring 越界（end 已夹到 len）、`clxiong.go:380-384` 与 `dy4k.go:700-724` 的数据竞争（各 goroutine 只碰自己的 index）、`erxiaopan.go:226`/`hjzhencai.go:184` 的 `[1]` 下标、`weibo`/`panlian`/`qqpd` 的路径穿越（gin 单段参数限制）。

### 5 方向审计回收完毕：合计高危 13 条（已修 3 条）、中危 20+ 条、存疑 24 组、复核排除 4 组

---

## 十九、统一 JSON 实现：清理 14 处绕过封装 + 守卫

项目约定业务代码只用 `pansou/util/json`（sonic 封装），但仓内有 **13 个业务文件**仍在直接
`import "encoding/json"`（另 3 个文件是混用：同一个文件里既用封装又用 stdlib）。

先给封装补齐调用方真正需要、但原先没暴露的能力，否则"只用封装"这条约定无法在全部调用点成立：

| 补齐项 | 说明 |
|---|---|
| `RawMessage`、`Number` | 别名 stdlib 同名类型，sonic 原生支持 |
| `NewDecoder`、`NewEncoder`、`Decoder`、`Encoder` | 注意 **sonic v1.14 的 `NewDecoder` 返回值而不是指针**，与 stdlib 不同，靠编译探针确认 |

迁移覆盖 14 个文件（`Marshal`/`Unmarshal`/`NewDecoder`/`NewEncoder`/`RawMessage`/`Number`
共 33 处调用）。三处混用文件采用"删掉 stdlib 导入、把调用改到已有封装别名上"的方式，避免重复导入。

现在全仓只剩 `util/json/adapter.go` 一处 `encoding/json`，而它的职责正是别名 stdlib 类型、
让调用方不必自己引。新增守卫用例 `TestNoDirectEncodingJSONOutsideWrapper` 扫描全仓强制这条约定
（对照实验：往 `plugin/sdso/sdso.go` 注入一行导入，守卫立即失败并点名该文件）。

`util/json` 覆盖率同时从 **0.0%** 提到有实际断言（编码解码往返、RawMessage 解到字段、
`UseNumber` 下大整数不丢精度）。

### 一次"纠正的纠正"——记录在此以免后人重犯

`panlian` 的重复 json tag 我最初判断"两个字段都被静默丢弃"。启用 JSON 统一后我怀疑该结论
依赖 `encoding/json` 语义、而生产走 sonic，于是跑了个探针看到 `TypeName="科幻片"`，据此写下
"sonic 不会丢弃，我的原判断错了"。

**这次纠正本身是错的**：那个探针跑在**已经修好 tag 的结构**上，根本没构成冲突，所以它什么
也没证明。重新用真正冲突的结构实测：

```
修复前结构（Type 与 TypeName 同为 json:"type_name"）+ sonic:  Type="" TypeName=""   ← 两个都丢
修复后结构（Type 为 json:"type"）+ sonic 给两个 key:           Type="电影" TypeName="科幻片"
```

结论：**sonic 与 encoding/json 在这一点上行为一致**，最初判断成立，改动确实修复了真实数据丢失。

教训（第 5 次同类）：探针必须真的触发被断言的条件。前面 4 次（`u3c3`、`erxiao` 域名、
`gying` 站点、自造的 3 秒批次预算）都是"从小样本/单点推广"，这次是"用不触发条件的探针推翻
正确结论"，方向相反但根因相同。

顺带把该用例的冲突结构改用 `reflect.StructOf` 在运行时构造：源码里写两个同 tag 字段会触发
`go vet` 的 struct field repeats json tag，运行时构造同样走到 sonic 的字段解析逻辑，
且保持 vet 零告警。

---

## 二十、缓存键正确性（审计 H1/H2/H4 已修）

| # | 问题 | 位置 | 状态 |
|---|---|---|---|
| 4 | 插件缓存键漏 ext：同一关键词同插件，ext 不同（结果也不同）的两个请求共用一条缓存 | `plugin/plugin.go`（原 `fmt.Sprintf("%s:%s", p.name, keyword)`）、`util/cache/cache_key.go` 的 `GeneratePluginCacheKey` | **已修** |
| 5 | 列表编码无边界：`["a","b"]` 与 `["a,b"]`、`["ab","c",…]` 与 `["a","bc",…]` 同键 | `util/cache/cache_key.go` | **已修** |
| 8 | `channelHashCache`/`pluginHashCache` 只增不删 | `util/cache/cache_key.go` | **已修**（上限 4096，到达后整体重置） |

**ext 摘要**落在 `util/ext_digest.go`。放 `pansou/util` 而不是 `util/cache` 是因为
`util/cache` 依赖 `pansou/plugin`，而基础插件包也要用它，放两者共同的下游才不会成环。
两个实现要点：

- 不直接 `Marshal` 整个 map——`pansou/util/json` 的 sonic 配置里 `SortMapKeys` 为 `false`，
  序列化 map 的键序不稳定，同一份 ext 会算出不同摘要，缓存将永远命中不了。改为键排序后
  逐项"长度:内容,"编码。
- 排除 `_ctx`（本次请求的 context 对象，随请求而变且不可序列化）与 `refresh`
  （"绕过缓存"开关而非结果形状参数）。`util.ExtContextKeyName` 是为避免导入环而重复声明的
  常量，`plugin` 包有一条断言用例锁住它与 `plugin.ExtContextKey` 一致，防漂移。

键格式抽成了 `pluginCacheKey()` 单一出处：既有测试 `plugin_fallback_test.go` 原本自己手拼
`name:keyword`，我一改格式它就失配——这正是"格式散落多处"的代价，顺手收掉。

**列表编码**改为 `encodeList`（逐项 `长度:内容,`），`calculateListHash` 也改为对它的结果取摘要；
原先 `strings.Join` 与逐项 `h.Write` 都有边界问题。

验证：`util` 6 个用例（区分结果形状参数、键序无关、拼接无边界碰撞、忽略 `_ctx`/`refresh`、
nil/空等价、常量一致性）、`util/cache` 5 个用例（含对照断言：先确认旧编码确实碰撞，否则判定
用例无意义）。另新增 `TestPluginCacheKeyIncludesExtDigest`，其中一条断言模拟"键里没有 ext"的
状态，证明摘要相同即塌缩成同一键——那就是修复前所有请求的处境。

### H3 也已修：磁盘→内存回填改按剩余寿命

`enhanced_two_level_cache.go` 的磁盘命中分支原先把 `config.AppConfig.CacheTTLMinutes`
（默认 60 分钟）当作内存 TTL，而内存缓存的 `SetWithTimestamp` 是 `expiry = now + ttl`
——重新计时，不是从原始写入时刻算。于是按短 TTL（`CachePartialTTLMinutes`，默认 3 分钟）
落盘的部分结果，一旦被挤出内存再从磁盘读回，就获得完整 60 分钟寿命；service 侧读缓存
又不校验新鲜度（`search_service.go` 注释明写"不检查新鲜度"），短 TTL 分流被整个绕过。

磁盘元数据里本来就有 `Expiry`（`disk_cache.go:152` 写入时按 ttl 算出的时刻），只是一直
没人用。新增 `DiskCache.GetExpiry` / `ShardedDiskCache.GetExpiry`（走内存元数据表，与
`GetLastModified` 同路径，无额外 I/O），回填时按 `time.Until(expiry)` 计时；若剩余已为负
则直接判为未命中，不复活过期条目。

验证：`TestDiskRefillUsesRemainingTTL` 走真实链路（短 TTL 写两级 → 删掉内存项制造磁盘命中
→ Get 触发回填 → 等过短 TTL → 内存里必须已失效）。对照实验：还原旧回填逻辑，该用例立刻
以"短 TTL 的结果在内存里活过了自己的寿命"失败。

### 缓存项收口（原写"仍未修"，现均已修，保留此处作为对照）

- **H6 磁盘写非原子**：已修——`os.CreateTemp` + `os.Rename` 原子替换。
- **H7 删除与元数据错误被丢弃**：已修——`Delete` 错误透出；`GetLastModified` 失败按当前时间
  回填并记录（原先零值时间会让回填的条目"看起来比实际老得多"）；磁盘写失败改为限流日志。
- **M3 主缓存读-改-写非原子**：已修——`main_cache_lock.go` 按键互斥，并由
  `TestMergeIntoMainCacheConcurrentKeepsEveryPluginResult` + 对照实验（不加锁时 16 个插件只剩
  1 条结果）锁住。

---

## 二十一、共享插件实例上逐请求写字段（并发 H1 已修）

插件实例来自全局注册表，跨请求共享。service 层此前每个请求都往同一实例上写：

```go
plugin.SetMainCacheKey(cacheKey)     // search_service.go 批任务与补齐路径各一处
plugin.SetCurrentKeyword(keyword)
```

而读点有两类：插件调用点普遍传 `p.MainCacheKey` 给 `AsyncSearchWithResult`（**106 处**），
框架内部还有 3 处读 `p.currentKeyword`。两个并发请求（或请求与后台补齐）互相覆盖，
A 关键词的结果会被写进 B 关键词的缓存槽。

讽刺的是 `plugin/plugin.go` 顶部的注释早已为 `ExtContextKey` 修过同一隐患——
"放在 ext 里传递而不是保存在插件实例上，是为了避免并发请求共享同一个插件实例时互相覆盖"
——却把这两个字段留在了实例上。修法就照那条注释办：

- 新增 `plugin.ExtMainCacheKey`，service 把它注入每份 per-request 的 ext 副本
  （`pluginExtWithContext` 现在一并注入上下文与主缓存键）。
- 框架在 `AsyncSearch` / `AsyncSearchWithResult` 两个入口 `resolveMainCacheKey(mainCacheKey, ext)`：
  **以 ext 为准，实例字段只作兜底**。这一改覆盖了全部 106 个插件调用点，插件侧一行未动。
- 关键词不再从实例读：`updateMainCacheWithFinal` 补上 `keyword` 参数（它原先没有，所以只能读
  `p.currentKeyword`），3 处读点全部改用参数透传；`SetCurrentKeyword` 标注 Deprecated。
- `util.ExtDigest` 把 `_main_cache_key` 排除：它是"结果写到哪"的地址而非结果形状参数，
  且其值本身由 keyword+plugins+ext 推出，算进摘要纯属冗余。

service 侧对共享字段的写入已清零（grep 确认 0 处）。

**对照实验（决定性）**：把 `resolveMainCacheKey` 退回"只用传入值"，并发用例里**每个**关键词
都收到同一个 `"key-for-WRONG"` —— 所有请求共用一个错误缓存键，正是串台本身。恢复修复后通过。

验证：`TestResolveMainCacheKeyPrefersExt`（兜底顺序、坏值/空串/类型不符不被带偏）、
`TestConcurrentSearchesKeepTheirOwnMainCacheKey`（6 个关键词并发，各自 ext 携带自己的键，
同时故意往实例写"别人的"键模拟修复前行为）。该用例首次运行暴露过一个真实细节：
`AsyncSearch` 在响应超时内完成走同步 updater、超时则交给后台完成路径（异步），
所以断言前需等结果到齐，否则会因调度抖动漏判。

### 顺带修掉的两处自身疏漏

- `pluginCacheKey` 辅助函数此前**只有测试在用**，生产两处仍是内联 `fmt.Sprintf`：
  早先那次替换因脚本后段断言失败而整批未写入，我当时只看到"辅助函数已加入"就以为完成了。
- 该函数的引入本身就是为了消除"键格式散落多处"，结果自己先散落了两处。

---

## 二十二、请求侧资源上限（输入鲁棒性 高 3/4/5 已修）

认证默认关闭（`AUTH_ENABLED` 未设置即 false），所以下面三个入口对任何可达客户端开放。
新增 `api/limits.go` 集中声明上限，超限**返回错误而不是静默截断**——静默截断会让调用方
以为请求被完整处理了。

| 入口 | 原先 | 现在 |
|---|---|---|
| POST `/api/search` 请求体 | `c.GetRawData()` 即 `io.ReadAll(Request.Body)`，无任何上限，整包物化进内存 | 先看 `Content-Length` 快速拒绝 413，再用 `http.MaxBytesReader` 兜住无长度头的分块请求，上限 1 MiB |
| `channels` | 只做 Split+TrimSpace；而它被当作 TG 路径工作池的 `maxWorkers`，直接决定 goroutine 数与出站请求数 | 上限 512，超出返回 400 |
| `/api/check/links` 的 `items` | 只拦空数组；每项触发一次上游请求 | 上限 256，超出返回 400 |

取值都远超合法用法（正常搜索请求几百字节、一个部署的频道数通常几十个），只在被滥用时生效。

边界判断（`>` 而不是 `>=`）抽成 `channelsOverLimit` / `itemsOverLimit` 两个函数并单独测试：
走完整 handler 才能测边界的话会进到全局 searchService 与真实上游调用，不适合放进单测，
而边界语义恰是这个判断唯一的风险点。

**顺带一件值得记的事**：新写的 `api/limits_test.go` 一开始用了 `encoding/json`，
被上一批加的守卫 `TestNoDirectEncodingJSONOutsideWrapper` 当场拦下并点名。约定有了机械保障，
第一个撞上它的就是我自己。

`api/` 包覆盖率同时从 **0.0%** 起步（5 个用例：超限请求体、超限频道、超限 items、
边界放行、空 items 语义不变）。

---

## 二十三、日志凭据、URL 转义、死配置字段（安全 中 2/3/8 + 配置 H3 已修）

**代理凭据入日志**：`main.go` 启动时直接打印 `config.AppConfig.ProxyURL`，而代理地址常写成
`http://user:pass@host:port`，等于把凭据写进日志与容器日志采集链路；同仓 `util/http_util.go`
里 `describeProxy` 专门做了脱敏，只是没被主流程调用。新增 `util.MaskProxyURL`：剥掉 userinfo
并保留 host:port（运维仍能看出用的是哪个代理），**解析不出来时返回占位文案而不是原文**——
拿不准就宁可不打。三处调用点（PROXY / HTTP_PROXY / HTTPS_PROXY）全部改用它。

验证（端到端）：`PROXY='http://alice:s3cr3t@proxy.example.com:8080'` 启动，日志输出
`使用HTTP代理 (PROXY): http://proxy.example.com:8080（凭据已隐藏）`，全文 grep 无 `alice`/`s3cr3t`。

**channel 未转义**：`util/http_util.go` 的 `BuildSearchURL` 里 `"https://t.me/s/" + channel`，
而 channel 直接来自请求参数，可注入 `?`/`#`/空格污染路径与查询串（同函数下面的 keyword
是转义过的，channel 漏了）。改用 `url.PathEscape`；合法频道名只有字母数字下划线，
PathEscape 对它们是无操作，不影响正常搜索（有用例锁住这一点）。

**两个死配置字段**：`Config.UseProxy`（配置层赋值 `proxyURL != ""`，全仓无读取点，代理判定
实际走 `ProxyURL != ""`）与 `Config.PluginTimeoutSeconds`（实际超时走 `PluginTimeout`）。
两者连同赋值一并移除。`SHARD_COUNT` 则是"只活在 README 里"的开关（分片数实际由 CPU 核数算），
属于文档承诺不存在，待改文档。

---

## 二十四、DelayedBatchWriteManager 统计字段跨锁域竞争（并发 H2 已修）

`WriteManagerStats` 里两类字段的写法不一致：计数器用 `atomic.AddInt64`（对的），
元信息字段 `LastFlushTime`/`LastFlushTrigger`/`LastBatchSize`/`TotalOperationsWritten`
却是裸读写——它们是 `time.Time`/`string`/`int`，无法直接用 atomic，于是被漏下了。
写侧有两个发起方（`flushGlobalBuffer` 由 handler goroutine 与监控 goroutine 都会调，
以及 `executeBatchWrite`），读侧是批处理 goroutine 的 `shouldTriggerBatchWrite`，
没有共同锁域。

修法：新增专用 `flushMetaMutex`（**不与 `queueMutex` 混用**，否则会把 flush 路径也串行化），
写走 `recordFlushMeta`/`addOperationsWritten`，读走 `lastFlushTime()`。

**顺手挖出更隐蔽的一处**：`GetStats` 与 `GetWriteManagerStats` 都是 `stats := *m.stats`
整结构复制。复制不是原子的，会与 `atomic.AddInt64` 的写入竞争——比裸字段更难看出来。
改为 `snapshotStats()` 逐字段 `atomic.Load*` 读计数器、元信息走锁。

验证（对照实验决定性）：`TestFlushMetaConcurrentAccess` 在 `-race` 下跑 2 写 2 读；
把 `recordFlushMeta` 临时改回裸写，`-race` 立刻在 `delayed_batch_write_manager.go:958`
报 `WARNING: DATA RACE` 并给出两侧调用栈（含用例的 36/40 行）；恢复后 `-race` 干净通过。
`TestFlushMetaRoundTrip` 另外锁住语义没被锁改坏（写入后读得到、时间确实前进）。

---

## 二十五、主缓存读-改-写非原子（缓存 M3 已修）

`injectMainCacheToAsyncPlugins` 注入的 `cacheUpdater` 干的是：

```
Get(key) → 反序列化 → mergeSearchResults(现有, 新增) → 序列化 → Set(key)
```

同关键词下多个插件并发完成时（异步插件的常态），两个调用读到同一份旧值、各自只把自己
那部分并进去再写回，**后写的覆盖先写的**——先完成那个插件的结果凭空消失，表现为
"明明搜到了，结果里却只剩一个源"。

修法：整段临界区纳入按键分条的锁（`service/main_cache_lock.go`，64 条，按缓存键内容做
FNV 哈希）。用分条而不是单把大锁：只有同一个缓存键的更新需要互斥，不同键之间不该排队，
否则一个慢键会拖住所有关键词的缓存更新。

**端到端测试的困难（如实记录）**：直接断言"并发合并不丢结果"需要驱动插件完成路径
（`AsyncSearch` 的同步/后台完成分支 + 工作池）。尝试过程中两次踩到真实机制：

- 用**同一个插件实例**并发 8 次不行——插件级缓存键含插件名，会被去重成只搜 1 次；
  真实场景必须是 8 个不同名插件写同一个主缓存键。
- 改成 8 个不同名实例并走注入路径后，探针显示 `cacheUpdater` 始终未被调用，排查耗时已
  超过收益，遂停手。因此本次测试覆盖的是**锁的语义**（同一键串行、不同键不互斥、
  解锁后放行、等值字符串落到同一条），而非端到端合并。

对照实验：把 `lockMainCacheKey` 换成空操作，`TestLockMainCacheKeySerializesSameKey` 立刻
以"同一个缓存键上有两个持有者同时进入临界区"失败。

**复核方式（部署环境）**：多插件并发搜索同一热词，观察 `merged_by_type` 各源条数是否与
逐插件单独搜索的条数之和一致；修复前会随机少源。

---

## 二十六、磁盘缓存写入原子化（缓存 H6/H7 已修）

`DiskCache.Set` 直接 `ioutil.WriteFile` 到最终路径。进程在写一半时被 kill（容器滚动更新、
OOM、宿主机重启）会留下**截断的文件而元数据仍然有效**，`Get` 于是把半截 JSON 交给反序列化
——故障表现是"结果是空的/残缺的"而不是缓存未命中，排查时极易误判成上游插件故障。
`saveMetadata` 同样非原子，半截元数据会让缓存项"存在但读不出"。

修法 `writeFileAtomic`：写临时文件 → `fsync` → `rename`。rename 在同一文件系统内原子，
读者要么看到旧内容、要么看到完整新内容；`fsync` 防止"rename 已生效但数据还在页缓存"时
断电留下空文件。`Set` 与 `saveMetadata` 都改用它。

**顺手删掉一处多余操作**：原代码在写新数据前先 `os.Remove` 旧文件。但 `getFilename` 是键的
MD5，新旧文件**同路径**，rename 本就会整体覆盖——预先删除非但无用，反而把"写一半崩溃"的
窗口重新打开，且删不掉时错误被丢弃。

**错误不再静默**：`Set` 里元数据保存失败后的清理、`Delete` 里两个文件的删除，原先
`os.Remove` 返回值全部丢弃。现在 `IsNotExist` 视为正常（过期清理、重复删除都会走到），
其它错误返回或打印——否则会变成"元数据已删但文件永远留在磁盘上"的静默泄漏。
`Delete` 无论文件是否删干净都要更新内存元数据表，否则内存会一直引用不存在的项。

**测试的边界（如实说明）**："崩溃窗口期不可见半截数据"这一点是**由构造保证**（rename 原子性），
不是测出来的——单测无法在任意时刻 kill 自己。用例锁住的是这个构造所依赖的不变量：
整体替换、失败时保留原内容且不留临时文件、目录不存在时报错而非静默成功、Set/Delete 后
无 `.tmp` 残留、并发读写时命中值只可能是两个完整载荷之一（200 KiB 载荷交替写 + 3 读，
回归网）。

对照实验：把 `writeFileAtomic` 换成直接 `os.WriteFile`，失败路径的两个用例
（保留原内容+指明失败环节、目录不存在时报错）立刻失败。

**已知残留（次要）**：被 kill 留下的 `.tmp` 文件不会被 `loadMetadata` 计入 `currSize`
（它按 `<name>.meta` 查找，找不到就跳过），也不会被 LRU 清理，属可见但无害的空间泄漏；
下轮可加启动清理。

---

## 二十七、循环内 defer 关闭响应体（资源 中项已修，且**修正了审计数字**）

审计记的是"5 处循环内 `defer resp.Body.Close()`"。用花括号深度写了精确检测器后，**词法上**
落在循环体内的候选是 **10 处**，但逐处定性后：

| 定性 | 数量 | 位置特征 |
|---|---|---|
| **真缺陷（已修 4 处）** | 4 | `for i := 0; i <= p.retries; i++` 重试循环体内直接 `defer` + `continue`。重试 N 次就同时压着 N 个未关闭的响应体，连接也不归还连接池。quarksoo / quark4k / bixin / pan666 |
| 误报：分支内立即 return | 4 | `if err == nil && resp.StatusCode == http.StatusOK { defer resp.Body.Close(); ...; return }`——defer 当场触发。gaoqing888 / dyyjpro / quarktv / lingjisp |
| 误报：每轮即时调用闭包 | 2 | 循环体是 `func(page int){...}(page)`，defer 作用域就是那一轮。sousou / hunhepan |

所以审计的"5 处"两头都不准：候选数不对，而且 6/10 是正确代码。**"循环里出现 defer"不等于缺陷**
——判据是 defer 的作用域是否被限制在单轮内（分支内 return 或每轮闭包）。

4 处改法是读完立即 `resp.Body.Close()`，并把该分支的注释写明原因，避免后人照着别处改回 defer。

**防回归守卫**：`main_body_close_guard_test.go` 的 `TestNoDeferBodyCloseInsideRetryLoop`
只认"循环体内且该循环体内存在 `continue`"这一种形状——刻意窄，因为另外两种相似写法是正确的，
不能误伤。对照实验：把 pan666 的修复还原回 `defer`，守卫立刻 FAIL 并点名
`plugin/pan666/pan666.go:213`。

**本条已修（原写"仍未做"已过时）**：72 处上游响应体读取已全部封顶（§二十八），
解压路径另加第二道约束（§五十）。剩余 12 处 `io.ReadAll` 已逐个核实为解码链或已封顶，
其中 `ash.go:105` 实际用的是 `limitReader`（早前扫描的假阳性）。

---

## 二十八、上游响应体读取封顶（资源 中项已修，77 处）

全仓 `io.ReadAll(resp.Body)` **恰好 72 处**（审计数字正确），另有 4 处别名的响应体
（`jsResp`/`challengeResp`/`infoResp`/`qrResp`）与 1 处在 `util/http_util.go` 内，合计 77 处。
上游是第三方网盘站点，返回体量完全由对方决定，而**插件是并发跑的**——几十个插件同时撞上
一个异常大的响应就足以把进程内存打满。这不是假想：返回体既可能被投毒，也可能只是页面
异常膨胀。

新增 `util.ReadAllLimited(r, limit)` 与 `MaxUpstreamResponseBytes = 16 MiB`。取 16 MiB 是因为
正常插件响应在一两 MiB 量级，留了一个数量级余量；真出现超过它的合法响应，宁可让该插件按
"读取失败"报错也不要静默撑爆内存——**失败是可见的，OOM 不是**。

实现要点：用 `io.LimitReader` 读 **limit+1** 字节。多读的那 1 字节是用来区分"正好等于上限"
与"超过上限"的——只读 limit 字节时无法判断流是结束了还是被截断了。用例同时验证了边界
（正好等于上限成功、多 1 字节失败）与"超限即停"（用一个读不完的流，读完就会耗尽内存）。

**迁移过程本身暴露了两个问题**，都不是我事先想到的：

1. 我的迁移脚本按 `package util` 跳过文件，于是 `util/http_util.go:298` 那处**漏了**——
   而它恰恰是所有插件共用的基础工具函数。是守卫测试第一次运行时报出来的。
2. 守卫第一版被**我自己的注释**误伤：`http_body.go` 里那句"全仓原先有 70+ 处裸
   `io.ReadAll(resp.Body)`"被当成违规代码。已改为先剥行尾注释再匹配。

迁移 49 个文件、77 个站点，同时处理了 28 处新增 `pansou/util` 导入与 42 处因 `io` 不再使用
而失效的导入。所有站点的错误处理原样保留（`body, err := ...` + `if err != nil`），错误语义
由 `ReadAllLimited` 包装后仍是可判定的（`ErrResponseTooLarge`）。

对照实验：守卫 `TestNoUnboundedUpstreamBodyRead` 在植入一处裸读后立刻 FAIL 并点名
`plugin/weibo/weibo.go:1131`。**注意该守卫的局限**：它扫的是源码文本，若违规文件同时缺
`io` 导入，会先编译失败而不是被守卫抓到——即守卫只在能编译的树上生效。

**未动**：38 处非 Body 的 `io.ReadAll`（本地文件、字符串）与 4 处测试内的
`io.ReadAll(request.Body)`（那是测试用的假服务器读请求体）。这些的输入不由外部决定。

---

## 二十九、TLS 校验开关与兑现 SHARD_COUNT（安全 中项 + 配置 H3 余项）

### 5 处硬编码 `InsecureSkipVerify: true`

qqpd ×4、panyq ×1。硬编码 true 等于把这个插件的返回内容对所有中间人开放，而本服务拿到的
就是搜索结果。修法不是"改成 false"（那会直接打断这两个插件），而是**把选择权交回部署方、
默认安全**：新增 `INSECURE_SKIP_TLS_VERIFY`（默认 false）与 nil 安全的 `config.AllowInsecureTLS()`，
5 处全部接入。

**实测这几处本来就不必要**：qqpd 的接口是腾讯域名（`xui.ptlogin2.qq.com`、`ptlogin2.pd.qq.com`、
`pd.qq.com`），走代理逐个校验证书 `ssl_verify_result=0`（有效）且返回正常状态码 404/302/200
——即默认开启校验后 qqpd 照样工作。`panyq.com` 在本机不可达（连接失败，非证书错误），
无法判定，风险由此交给部署方用开关决定。

注意 `util/http_util.go:133` 的 `InsecureSkipVerify: false` **不是**同类问题，那是正确的。

### SHARD_COUNT：把文档承诺兑现

README 一直把 `SHARD_COUNT` 列在环境变量表里，而代码从来没读过它——分片数由
`runtime.NumCPU() * 2` 算。文档承诺了一个不存在的开关。选择**实现**而不是删文档：用户的
预期是"设置它有用"，删文档是改了约定而不是兑现约定。

`resolveShardCount`：显式值优先，非 2 的幂向上取整（分片按掩码取模，必须是 2 的幂），
一律夹到 [4, 64]；非法值忽略并打印回落原因——免得一个手滑的 `SHARD_COUNT=0` 变成零分片缓存。

验证：`config` 2 个用例（默认 false、nil 安全、只认 true/1）、`util/cache` 3 个用例
（生效、非 2 的幂取整、0/负数/非数字/空白回落、超大夹到 64、超小抬到 4、未设置按 CPU 推算、
多核封顶）。README 环境变量表同时补上两个开关的准确语义。

## 三十、清理上次遗留的 .tmp（上一轮引入的残留，已闭环）

原子写用 `os.CreateTemp` 造临时文件，进程被 kill 时可能留下它。这些文件不会被
`loadMetadata` 计入 `currSize`（它按 `<name>.meta` 查找，找不到就跳过），也不会被 LRU
清理——不清就是永久占着磁盘。启动加载时扫描并删除，删除失败打印原因（可能是权限问题）。

对照实验：把匹配条件改成永不匹配，用例立刻以"残留临时文件未被清理：
deadbeef.tmp123456 / deadbeef.meta.tmp654321"失败；同时验证清理不会误伤正常缓存项。
（第一次做对照时我把整段删掉，结果 `strings` 变成未使用导入、编译失败——那是编译错误
而不是用例命中，据此换成了"永不匹配"的写法。）

---

## 三十一、重试实现收敛：公共组件 + 首个试点（33 处中已迁 1 处）

全仓 30 多处复制粘贴的重试循环形态高度一致：`for i := 0; i <= p.retries; i++`、固定
`time.Sleep(500 * time.Millisecond)`、每处都要自己处理"最后一次不再等待""错误怎么包装"
"响应体在循环里怎么关"——其中 4 份就是在这里把 `defer` 写进循环体（见 §二十七）。

新增 `util.DoWithRetry(cfg, fn)`：
- `RetryConfig` 支持固定间隔（`BaseDelay == MaxDelay`，即既有形态）与指数退避 + `MaxDelay` 封顶。
- `Jitter` 加 ±25% 抖动。**抖动不是装饰**：全仓写死 500ms，上游一旦限流，所有并发插件会在
  同一时刻重新打过去，把限流变成雪崩。
- `OnRetry` 回调供观测；返回的错误保留原始错误链（`errors.Is` 仍可判定），只在最外层加上
  尝试次数。
- 成功路径不等待（有用例锁住，否则每次正常请求都会被拖慢）。

首个试点 quarksoo 迁移后**行为逐项对齐**并实测：3 次尝试（`retries=2` +首次）、
总耗时 1.01s（固定 500ms ×2）、耗尽后返回 `重试 3 次后仍失败: API返回非200状态码: 500`
（保留上游原因）、返回条数 0。用例覆盖"先 500 两次再 200 应成功"与"一直 500 应报错且
尝试次数正确"。`BaseURL` 由 const 改为 var 以便测试指向本地假服务器。

### 更正：那个"0 请求 / nil 错误 / 0.00s"的真实原因（我连续误判了两次）

第一次判成"缓存未按关键词隔离"，第二次判成"框架缓存命中属既有设计"。**两次都错。**

真正的原因是 **`config.AppConfig.AsyncResponseTimeoutDur == 0`**：这种配置下框架会**立刻返回
空结果且不报错**，搜索转到后台继续跑。于是用例看到的是"零请求、nil 错误、0.00s"——三个特征
全都指向"什么都没发生"，而实际上请求在后台发着。这个签名极易被误判，我已把它写进用例注释。

定位方式来自 YG 的提示："可以直接清理数据缓存 cache 目录来测试，这样不换关键词也可以"。
照做后发现**只要把缓存目录指向用例专属的临时目录、并把 `AsyncResponseTimeoutDur` 给足，
同一关键词、不带 `refresh` 也能稳定跑通**（`-count=2`：4.04s = 2 轮 × 2 用例 × 1s，请求都发生）。
这也顺带说明：失败结果不会被缓存（否则同关键词的第二个用例仍会命中），所以此处根本不涉及
关键词隔离问题。

最终用例简化为"同一关键词 + 独立缓存目录"，去掉了为绕开"缓存"而加的关键词区分与
`refresh`——那两样都是基于错误诊断加的。教训与 §二十五 那次同类：**先把现象判成缺陷之前，
先确认触发条件真的成立**（这次连"是缓存"这个前提都没验证）。

**最初的下错结论记录如下（保留以备对照）**：第二版用例出现"0 次请求、nil 错误、0.00s"，我一度
判断是"缓存未按关键词隔离"的缺陷。实际查明是**框架的既有设计**——同一关键词的结果（含
空结果）会被缓存，命中即不发请求，这是缓存正常工作。两个用例都必须显式带 `refresh: true`
并且用不同关键词，否则测的是缓存而不是重试。结论：**在把现象判成缺陷前，先排除"这是
设计如此"**。

剩余 3 处同形循环（`grep 'for i := 0; i <= p.retries'`）与其它形态的重试共 30 余处待迁移，
属机械替换，但每处的错误文案与循环后用法不同，需逐个人工确认——不做批量替换。

## 三十二、重试迁移完成同形批次（quarksoo + bixin + pan666 + quark4k）

`grep 'for i := 0; i <= p.retries'` 现已为 **0**：形状完全相同的四个插件全部迁移到
`util.DoWithRetry`，参数保持既有行为（固定 500ms、`p.retries+1` 次尝试），错误文案逐字保留
（`请求失败` / `读取响应失败` / `API返回非200状态码`），只是最外层多了一层"重试 N 次后仍失败"。

迁移用按行替换完成（四处循环体连注释都一致，属真复制粘贴），替换后逐个编译验证；
`pan666` 补了假服务器用例并实测：**先两次 500 再 200 能成功**（说明重试确实生效）、
**一直 500 会报错而不是返回空结果**。pan666 的 `BaseURL` 由 const 改为 var 以便测试指向假服务器。

**测试又踩到两个坑，都记在用例注释里了**：
1. pan666 除搜索自身外还会打别的端点，按"总请求数 == 重试次数"断言会算错——改为
   "不少于重试次数"，精确次数由 `util/retry_test.go` 覆盖。
2. 我第一版假响应写成 `{"data":{"list":[]}}`，而该插件期望 `{"data":[...]}`，于是用例
   在"重试成功后解析失败"处报错。这其实**反证了重试生效**（它确实越过了两次 500），
   只是我的 fixture 不对。

其余形态的重试（非 `p.retries` 形状）仍在清单里，需逐处人工确认错误文案与循环后用法。

## 三十三、重试迁移第二批：返回响应的同源家族（mizixing + yiove）

`for i := 0; i <= p.retries` 那批是"读体后返回结果"；这批是**另一个复制粘贴家族**：
`for attempt := 0; attempt < maxRetries; attempt++`、返回 `*http.Response`（由调用方读体）、
已有指数退避 `retryBaseDelay * 2^attempt`、保留 `lastErr`。mizixing 与 yiove 的循环体**逐字相同**
（连我之前修的 `%!w(<nil>)` 注释都在，证实同源）。

迁移时保留语义：`Multiplier: 2` 对应 `2^attempt`；`DoWithRetry` 天然跳过最后一次等待，
等价于原先的 `if attempt < maxRetries-1`。失败时状态码仍被带出，`%!w(<nil>)` 那个回归锁进了用例。

**用例耗时本身就是退避语义的证据**：3 次尝试 0.60s = 200ms + 400ms（`retryBaseDelay × 2^attempt`），
2 次尝试 0.20s = 单次 200ms。

这个过程里我又犯了一次编辑错误：把原循环后的 `return nil, fmt.Errorf(...)` 用正则删掉时留下一个多余
的 `}`，且第一次修正因为没料到中间有个空行而匹配失败（`}\n\n}` 而非 `}\n}`）。两次都是先看到 `go build`
报 `expected declaration, found '}'` 才定位——**编辑脚本的断言必须打在真正的位置上，否则删多删少都
只会在编译期暴露**。

同家族的 kkmao、ypfxw（用 `backoff` 变量）、yulinshufa（收尾文案不同）、javdb（限流专用）、
gaoqing888/dyyjpro/quarktv（循环内 return + cancel 的另一种形状）仍需逐处迁移，形状不同不批量做。

## 三十四、重试迁移第三批：kkmao / ypfxw / yulinshufa

kkmao、ypfxw 与 §三十三 的 mizixing/yiove **同形**（只差延迟来源：一个是入参 `baseDelay`，
一个是常量 `retryBaseDelay`），循环体连注释都一致，故按同一模板迁移；区别只在 `RetryConfig.BaseDelay`
的取值。

yulinshufa 是**变体**，需单独对齐两处语义：
- 它把等待放在**尝试之前**（`if attempt > 0` 先睡再发），组件是失败后等。二者等价：原第 k 次尝试前等
  `2^(k-1)×200ms`，组件在第 j 次失败后等 `2^j×200ms` 再发第 j+1 次，逐项相同。所以
  `Multiplier: 2` + `BaseDelay: 200ms` 精确还原原退避曲线。
- 它的 debug 日志打在"即将重试"处、**次数从 1 起计**；组件回调的 `attempt` 是 0 起的失败序号，
  所以日志里要写 `attempt+2` 才是原来的"第几次请求"。这类差一错误若不对齐，日志就会从"第 2 次"
  开始，排查时误导人。

kkmao 补了行为用例，**耗时即证据**：3 次尝试 0.31s ≈ 100ms + 200ms，退避曲线仍生效；上游一直 403 时
错误里必须带 `403` 且不得出现 `%!w(<nil>)`。

至此同源家族（返回 `*http.Response`、带指数退避）已迁移 5 处。全仓 `for attempt := 0` / `for retry := 0`
形态仍有 26 处，形状各异（限流专用、循环内 return + cancel、先睡后试、固定间隔等），继续逐处核对迁移。

## 三十五、重试迁移第四批：gaoqing888（循环内建 context 的形状）

`fetchBody` 是另一种形状：**每次尝试各自 `context.WithTimeout`**，即超时按尝试计算、而不是整轮
共享一个。迁移时这个语义必须保留——ctx/cancel 建在闭包内（`defer cancel()` 在闭包返回时释放，
不会跨轮累积），而不是提到循环外。

其余语义同样逐项对齐：退避 `200ms × 2^attempt` → `Multiplier: 2`；成功即读体返回；
失败带 `HTTP %d` 状态码。原实现对"建请求失败"是立即返回不重试，迁移后会被重试——这个差异无害
（同样的输入必然同样失败），但记录在案。

补了 3 个用例，其中最关键的是**每次尝试的超时必须生效**：上游 handler 故意挂 2 秒，单次超时
100ms、3 次尝试，实测 `elapsed` 约 0.9 秒（300ms 超时 + 200ms + 400ms 退避），远小于上游的 2 秒
——证明确实按次超时而不是被拖成整轮。

**注意用例耗时不能直接当证据**：`httptest.Server.Close()` 会等未完成的 handler 结束，所以该用例
报告的 2.80s 里含服务器关闭等待的 2 秒；真正的证据是断言里单独测的 `elapsed`。前几批我用
"测试耗时 = 退避曲线"作证据时，恰好因为 handler 立即返回而没有这个偏差，但判读方式应统一为
**测量目标代码本身的耗时**。

## 三十六、重试迁移第五批（dyyjpro / quarktv / lingjisp）+ 补上手写无界读取守卫

dyyjpro、quarktv 与 §三十五 的 gaoqing888 同族（循环内建 context、超时按次计算），照同模板迁移。
lingjisp 同族但多一处差异，迁移时一并修掉。

### 守卫的漏洞：别名与手写实现能逃过文本匹配

§二十八 把 77 处上游响应体读取封顶 16 MiB，但守卫是按 `io.ReadAll(` **文本匹配**的。于是：

- `plugin/quarktv/quarktv.go` 里的本地包装 `ioReadAll` 自己 `make` 缓冲、在 `for {}` 里无限
  `append`，**完全逃过检查**，实际上限为零；
- 新守卫扫描后又抓到 `plugin/lingjisp/lingjisp.go:258` 的同一种手写循环。

两处都改为 `util.ReadAllLimited`。这正是"守卫依赖具体写法"的代价——它只能挡住它认识的那个
写法。新守卫改为按**语义形态**匹配：`for {}` 循环体内出现 `.Read(...)` 且紧接着 `append(...)`
累积，且整段未提到上限常量/LimitReader，即判为可疑。对照实验用已修复的那种代码作样本，
守卫必须报出；有界写法不得误报。

（新守卫第一版有个 bug：`filepath.Base` 作用在了**文件内容**而不是路径上，报错信息打出来是一大段
源码。已改为传入路径。这类"测试工具自身的错"必须靠看输出判断，不能只看通过与否。）

### 验证

quarktv 用例：先两次 500 再 200 能成功（0.60s ≈ 200ms+400ms 退避）；**超过 16 MiB 的响应必须报错**
——这条直接证明封顶生效，而不只是"代码看起来改了"。全仓扫描现为 0 命中。

## 三十七、重试迁移第六批：同源家族批量（mikuclub / jsnoteclub / duanjuw / alupan）

剩余 22 处先按**形态**分类（A：返回 `*http.Response` 12 处；B：读体返回 data 2 处；C：其它 7 处），
再用**同源标记**（`HTTP 状态码 %d` 文案，即之前修 `%!w(<nil>)` 时留下的痕迹）筛同源者，最后用
容错模板逐个核对——只有模板唯一匹配、且四项标记齐备（ok 分支 `return resp, nil`、状态码带出、
`lastErr = err`、退避 `1<<attempt`）的才批量迁移。

结果：12 处 A 族里 4 处（mikuclub、jsnoteclub、duanjuw、alupan）模板唯一匹配，一次批量完成，
`Attempts`/`BaseDelay` 从各文件原表达式提取（`maxRetries`/`duanjuwMaxRetries`、
`retryBaseDelay`/`duanjuwRetryDelay`），退避语义与原先的 `2^attempt` 一致。

**分批的判断依据必须来自代码本身，不是文件名**：javdb、jupansou、lou1、daishudj 同样带同源标记，
但模板不匹配（收尾或循环体有差异），因此**不并入本批**，留待逐个核对。

alupan 用例：3 次尝试 0.60s = 200ms + 400ms（退避生效）、上游一直 502 时报错且带 `502`、
不得出现 `%!w(<nil>)`。其余 3 处无测试文件，行为一致性由同一模板 + 共享组件用例保证，
此点如实记录——**批量迁移的验证强度低于逐个迁移**，这是它的代价。

## 三十八、重试迁移第七批：jupansou / lou1 / daishudj（改用括号配对的通用替换）

这三处与 §三十七 的模板只是**边缘差异**（jupansou 的退避写作 `200 * time.Millisecond * time.Duration(1<<attempt)`，
顺序与其他文件相反；另两处循环头/收尾的空行不同），精确模板匹配失败。改用**基于括号配对的通用替换**：
定位 `for attempt := 0;` 行 → 配平括号取循环体 → 从体内提取循环次数与退避基准表达式 → 替换
整个 `var lastErr error … return nil, …` 区间。这样不依赖空格与字面量写法，适用于同族的所有变体。

**又一次留下多余的 `}`**（`return nil, …` 那行不含函数收尾括号，而新块自带 `}`），三处都是靠
`go vet` 报 `expected declaration, found '}'` 定位。这已经是第二次同类问题，结论是：
**这类替换不需要额外写守卫——编译器就是守卫**，但替换脚本必须把"函数收尾括号是否被包含进区间"
当成显式约束，否则每次都要靠编译错误回头修。

### 同族≠同行为：4 处**不可**按模板迁移

对剩余 A 族逐个核对退避形状，发现它们根本不是同一种退避：

| 插件 | 实际退避 |
| --- | --- |
| xiaoyu | **线性**：`time.Duration(attempt) * 200ms` |
| yunsou | 错位指数：`2^(attempt-1) * 200ms`（先睡后试） |
| aikanzy | 用变量 `backoffTime` |
| zlxapp | **完全不睡**（无退避） |
| javdb | 限流专用：只在 429 时重试，`resp.StatusCode != 429` 即返回 |

这直接说明**不能按"A 族"批量迁移**：xiaoyu 若套用 `Multiplier: 2` 会把它从线性退避**改成指数退避**，
那是改行为不是收敛实现。zlxapp 压根没有退避语义，迁进带退避的组件同样是改行为。javdb 只在 429
重试的语义组件也不表达。这四处各自需要判断，不进本批。

## 三十九、RetryConfig 补 DelayFunc，据此迁移 yunsou 与 xiaoyu

### 为什么必须加 DelayFunc

§三十八 逐处核对退避形状后发现，"退避曲线"根本不是只有倍率一种：xiaoyu 是**线性**
（`attempt × 200ms`）、javdb 是**区间随机**（限流场景，故意打散重试时刻）、aikanzy 用变量、
zlxapp **完全不退避**。只提供 `BaseDelay × Multiplier` 一种表达，就等于逼着这些站点要么改行为、
要么永远留在组件外——两者都不可接受。

因此 `RetryConfig` 增加 `DelayFunc func(attempt int) time.Duration`，优先级最高（非 nil 时
`BaseDelay`/`Multiplier`/`Jitter` 全部不参与），返回 0 表示该次不等待。用例覆盖：线性三项取值
逐项校验、以及"返回 0 时真的不等待"（并实测重试耗时，避免只是配置写了而实现仍在睡）。

### 迁移

- **yunsou**：与 §三十四 的 yulinshufa 同形（先睡后试、`2^(attempt-1)×200ms`），用
  `Multiplier: 2` + `BaseDelay: 200ms` 精确还原。
- **xiaoyu**：用 `DelayFunc` 表达线性退避。映射关系写进注释：原实现第 k 次尝试前等 `k×200ms`，
  组件在第 j 次失败后等待再发第 j+1 次，故 `DelayFunc(j) = (j+1)×200ms`。次数保持 `maxRetries+1`
  （原循环条件是 `attempt <= maxRetries`，容易在迁移时误写成 `maxRetries`）。

**这一步的意义不是多迁了两个插件，而是把"异构退避"从"无法收敛"变成"可以收敛"**——javdb 的
区间随机、zlxapp 的零退避现在都有了归宿，只剩逐处确认调用语义。

xiaoyu 只有既有测试、无重试专项用例，此点如实记录。

## 四十、重试迁移第八批：aikanzy（附带修掉状态码丢失）、zlxapp

### aikanzy：迁移顺带修掉一个真缺陷

原实现只保存 `client.Do` 的 err，**状态码非 200 时 err 为 nil**，于是重试耗尽后报出的是
`重试 N 次后仍然失败: %!w(<nil>)`——真实状态码丢失、完全无法定位。这正是 §十九 在 41 处修过的
同一个问题，aikanzy 漏网。迁移时把状态码带出，并用例锁住（上游一直 404 时错误必须含 `404` 且
不得出现 `%!w(<nil>)`）。退避为 `backoffBase(200ms) × 2^(k-1)`，用 `Multiplier: 2` 还原；
实测 4 次尝试 1.41s = 200+400+800，与曲线一致。同时保留原"每次重试克隆请求"的意图。

### zlxapp：**更正上一轮的判断错误**

§三十八 我写"zlxapp 完全不睡（无退避）"——**这是错的**。它确实有退避，而且比我以为的更好：
`attempt × 200ms` 线性，用 `time.NewTimer` + `select` 实现，**等待可被请求上下文取消**。
错因是分类脚本只 grep 了 `time.Sleep(`，而它根本不用 Sleep。教训：**用单一文本模式给代码分类
会漏掉等价写法**，这正是前面"守卫只能挡住它认识的写法"同一个病根。

迁移时保住两个特点：线性退避留在闭包内（组件侧 `DelayFunc` 返回 0），以及可取消等待原样保留。
**已知差异如实记录**：上下文取消后组件仍会把剩余尝试次数走完（每轮立即失败），原实现是立刻返回；
差别只在取消瞬间，且不再有等待。

### 一个待补的组件能力

zlxapp 与 javdb 都提示同一件事：`RetryConfig` 的等待是 `time.Sleep`，**不可取消**。若给组件加
`WaitFunc`（默认 Sleep，可注入带 context 的实现），则这批"可取消等待"的站点能更自然地收敛，
也能让取消语义在全仓统一。已记入清单。

## 四十一、重试迁移第九批：javdb（A 族清零）

javdb 的语义与其它插件**都不同**，逐项对齐：
- **只在 429 时重试**：非 429（含 5xx）一律直接返回该响应，由调用方判断（用例锁住：非 429 时
  只请求 1 次、不置限流标志、返回原状态码）；
- 退避是**随机区间** `MinRetryDelay~MaxRetryDelay` 秒，故意打散重试时刻避免加压——用 `DelayFunc`
  表达，倍率退避在这里是错的；
- 次数 `MaxRetryOnRateLimit+1`（原 `attempt <= MaxRetryOnRateLimit`）；
- 重试用尽时置 `rateLimited` 标志、返回带原因的专用错误，而**不是**笼统的"重试 N 次后仍失败"。
  组件返回错误时优先返回这个专用错误，因为它带限流原因、更有用。

关键点：`MaxRetryOnRateLimit` 默认为 **0**，此时 `Attempts=1`，第 0 次即命中"放弃"分支——与原实现
"设为 0 则不重试"一致。这条最容易在迁移时写错成"仍重试 MaxRetryOnRateLimit 次"，用例专门锁住。

**A 族（返回 `*http.Response` 的 12 处）到此清零**：mizixing、yiove、kkmao、ypfxw、mikuclub、
jsnoteclub、duanjuw、alupan、jupansou、lou1、daishudj、javdb。余下 C 族 7 处 + 其它形态，
以及既有的吞错与端到端用例两项。

## 四十二、RetryConfig 补 AbortError，据此迁移 panlian；并给剩余重试**定性收口**

### 剩余 9 处逐个定性（结论：只有 1 处是"普通 HTTP 重试"）

| 插件 | 定性 | 依据 |
| --- | --- | --- |
| gying L1927 | **非 HTTP 重试** | 循环体无请求/无 Do |
| miosou L143 | **非 HTTP 重试** | 同上 |
| miosou L95 | **反爬门禁握手** | Anubis gate 失效后重取 token 再 `continue`，非通用重试 |
| pansearch L389 | **非 HTTP 重试 + 提前中止** | 同上 |
| ting77 L214 | **重定向回调** | 循环包的是 `CheckRedirect`，`errRateLimited` 短路 |
| erxiaopan L577 | 可取消等待 + **提前中止** | 4xx 直接 break（429 除外），且 ctx 取消即返回 |
| panta L1259 | **提前中止** | — |
| yuhuage L243 | **提前中止** | — |
| panlian L1271 | 真 HTTP 重试 + **提前中止** | 登录失效/解析失败不该重试 |

也就是说：**"33 份复制粘贴重试"这个条目的实际可收敛面已经走完**——24 处已迁，其余不是通用重试
（5 处）或受限于组件能力（4 处）。这不是"没做完"，而是**先前的数字把不同性质的东西算在了一起**。

### 新增 AbortError：把"不该重试"变成可表达

仓里的重试并不都是"任何失败都值得再来一次"：4xx（目标明确拒绝）、登录失效（cookie 过期）、
响应格式不对（再试还是不对）——继续重试只是白等。此前这 4 处无法收敛，只能各自保留循环。

新增 `util.Abort(err)` + `ErrAborted` 哨兵：
- `DoWithRetry` 遇到被标记的错误**立即停止**，不等待、不触发 `OnRetry`（用例实测：调用 1 次、
  重试回调 0 次、耗时 <200ms）；
- **不包装成"重试 N 次后仍失败"**——那会误导，因为它第一次就决定不重试了（用例显式断言）；
- `errors.Is` 既能拿到原始错误、也能判定 `ErrAborted`；
- `Abort(nil)` 必须是 nil（否则会把成功变成中止），单独有用例。

### panlian 迁移（首个 AbortError 消费者）

两处中止点逐一表达：登录失效（401 或响应体含"请先登录"/`admin_auth_required`）与解析失败。
退避是**线性** `(attempt+1)×200ms`，用 `DelayFunc` 原样表达；建请求失败也标为 Abort（重试没意义）。

**如实记录**：panlian 只有既有测试，本轮未补中止语义的专项用例（`AbortError` 本身的能力已由
`util/retry_test.go` 覆盖）。这个缺口留在清单上。

**编辑脚本第三次吞掉函数收尾括号**——这次区间终点是 `return lastErr`，而它后面还有一个 `}`。
三次都是靠编译器定位。这条已足够说明：**这类脚本必须显式断言"替换区间是否包含函数收尾括号"**，
我已把它写成固定规则，后续脚本先检查该行之后的首个非空行是否为 `}`。

## 四十三、吞错处置（一）：nsgame 半截数据当成功、磁盘写失败无痕

26 处吞错（23 处 `_ =` + 3 处 `X, _ :=`）逐处定性，本轮先处置文档点名的两处真缺陷。

### nsgame：读体失败返回"半截 data + nil error"

`data, _ := util.ReadAllLimited(resp.Body, ...)` —— 读体失败被丢弃，调用方拿到的是被截断的会话
数据却毫无察觉，还会被当成成功继续走。已改为取错误并返回。

顺带把顺序调正：**先判状态码再读体**。非 200 的响应体本来就要丢弃，先读它既浪费、又可能因为
错误页过大而报出"响应过大"，把真正有用的状态码盖掉——那会让排障信息更差。

### 磁盘缓存写失败：`_ =` 让它完全没有痕迹

`enhanced_two_level_cache.go` 的异步磁盘写是 `_ = c.disk.Set(...)`。后果很隐蔽：**内存里还有值，
一切看起来正常，直到重启后缓存全空，且没有任何线索**。

异步写没法把错误回传给调用方，所以处置是"让它可观测"而不是"返回错误"。但直接每次都打日志有害
——磁盘写满时每次 Set 都失败，会把日志刷爆、反而淹掉别的问题。因此加**限流**：30 秒窗口内只打
一次，并把期间累计的失败次数一并报出（**数量这个信息不能丢**）。用例锁住限流行为：首次打印、
窗口内累计计数、窗口外打印后清零累计。

### 一个测试自身的坑

我原本还写了一个"磁盘写失败时 Set 仍应成功"的用例，结果 panic。查明原因不是 `c.disk` 为 nil
（构造函数必然创建磁盘缓存），而是**构造函数依赖全局 `config.AppConfig`**，测试进程里为 nil。
也就是说：这个方向要测得先注入配置。无效用例已删除并如实记录原因——**测试失败时要先确认是
被测代码的问题还是测试环境的问题**，否则很容易把环境问题写成"发现缺陷"（本轮之前的 zlxapp
误判就是同类错误的另一种形态）。

## 四十四、吞错处置（二）：check_service 的两组，一真一假

### checkAliyun 的 5 处 `_ = utiljson.Unmarshal`：**不是缺陷**（有据）

文档把它们列为"解析失败后继续用零值结构体判定"。实际读完整判定链后确认**危害不成立**：

零值只会让 `code == ""`、`FileCount == nil`、`shareStatus == ""` 三个分支被跳过，最终落到
`case statusCode != http.StatusOK` 或 `default`，两者都返回 `checkStateUncertain`——
**解析失败不会伪装成"链接有效"或"链接失效"，它落在一个安全的不确定态**。

而且这本来就是**机会式解析**：aliyun 的响应可能是 HTML 而非 JSON，解析失败是正常情形，
所以判定必须同时依赖 HTTP 状态码与解析出的字段（函数签名里的 `statusCode` 正说明这点）。
把 `_ =` 改成"解析失败即报错"反而会破坏正常路径。

结论：保持原样，**记录为"非缺陷·机会式解析"**。这是本次治理里第四次"看起来像缺陷、查完发现
需要证据"的情形。

### bolt 读取事务：真吞错，已修

`_ = s.cacheDB.View(...)`：表现出来只是"缓存未命中"，看起来一切正常，**实际 DB 损坏/关闭时
每次检查都会重读失败、且没有任何线索**。已改为记录日志。

同时修掉内层更隐蔽的一处：`decodeCachedCheckEntry` 失败原本 `return nil`，把**解码失败也伪装成
未命中**——损坏的条目会被永久静默忽略。改为向上返回，由事务把它当错误交给外层记录。

## 四十五、吞错处置（三）：panlian 用户状态持久化 5 处

5 处 `_ = p.saveUser(user)`（搜索成功更新最后访问时间、登录失效置 expired、relogin 成功/失败、
状态查询时新建用户）。危害是**重启后才显现**：内存状态已更新，当前进程一切正常，但持久化失败时
更改会丢失——新登录的用户会变回 `pending`、relogin 拿到的 cookie 会消失——而且没有任何线索。

与 §四十三 的磁盘写属同一类：**操作本身已经成功（搜索成功、登录成功），错误没法回传给调用方**，
所以处置是让它可观测，而不是把错误抛出去（那会把成功的搜索变成失败，是更糟的取舍）。

抽成 `saveUserOrLog`，失败时打印并说明后果（"重启后可能丢失登录态"），避免 5 处各写一遍。
这里**不需要限流**：saveUser 的频率是"每次搜索成功/登录"，不是每次结果，不会刷爆日志。

行号注意：文档里记的 `panlian.go:671/681/1428/1490/1493` 在本轮迁移重试后已偏移（当前为
674/684/1439/1501/1504）。**按行号定位历史技术债在持续重构中会失效，应按代码形态搜索**。

## 四十六、吞错处置（四）：范围更正——多数 `_ =` 根本不是吞错

按代码形态（而非文档里的行号，见 §四十五）全仓重新扫描后，剩余的 `_ =` **绝大多数不是吞掉错误**：

- `magnet, _ = a.Attr("href")`、`title, _ = link.Attr("title")` 之类：goquery 的 `Attr` 第二个返回值是
  **`bool`（属性是否存在）**，不是 error。丢弃它是有意为之的常规写法。这类占绝大多数（当前 15 处）。
- `_, _ = h.Write([]byte(...))`：`hash.Hash` 的接口文档明确"永不返回错误"。
- `_ = writer.WriteField(...)` / `_ = writer.Close()`（jupansou）：multipart.Writer 写进的是
  `strings.Builder`，`Builder.Write` 不会失败，所以这些 error 在实践中不可能出现。

**结论：§十一 里"23 处 `_ =`"这个数字把不同类型的丢弃算在了一起。** 真正属于"错误被吞"的，
本轮及前两轮已全部处置（nsgame 读体、磁盘写、bolt 读取与解码、panlian 持久化 ×5、pansearch
时间解析、djgou 文档解析）；其余是 bool 与"不可能失败"的返回值。

### 本轮修的两处

- **pansearch 时间解析**：`datetime, _ = time.Parse(...)` 失败后时间戳变成**零值**，随后在按时间
  筛选时被**静默排除**——表现是"这条资源明明有却搜不到"。改为换一个常见格式再试，仍失败则记录
  原因并明确按"无时间"处理。
- **djgou 挑战页解析**：`doc, _ = goquery.NewDocumentFromReader(...)` 失败会留下半截 doc，后续
  用它找挑战字段只能得到"没找到"，**与"页面里确实没有挑战"无法区分**。改为解析成功才替换 doc，
  失败记录原因。

## 四十七、补齐 §四十二 留下的缺口：panlian 中止语义专项用例

§四十二 只验证了 `AbortError` 自身（`util/retry_test.go`），没验证它在真插件里的效果。本轮补上，
并借此验证 `Abort` 与普通重试的**分界**：

- 登录失效（401 + 响应体"请先登录"）：**只请求 1 次**、<150ms 返回（退避没生效，证明确实中止了）、
  且 `errors.Is(err, errLoginRequired)` 仍成立——中止没有破坏错误链；
- 普通 5xx：仍按 3 次尝试、0.60s = 200+400ms 线性退避重试——避免"用 Abort 把重试整体关掉"
  这种过度收敛。

`DefaultBaseURL` 由 const 改为 var 以便测试指向本地假服务器（与 pan666、quarksoo 同样处理）。

## 四十八、主缓存并发合并端到端用例（补上最后一个缺口）

### 为什么之前补不上：没有可测接缝

合并逻辑原先藏在 `injectMainCacheToAsyncPlugins` 里的 `cacheUpdater` 闭包中——要测它就得驱动整个
插件框架（早前尝试过：单例插件会因插件级缓存去重、注入探针显示 `cacheUpdater` 根本不被调用），
于是只能靠读代码下结论。**这是"缺可测接缝"，不是"缺用例"。**

### 处置：先抽函数，再写用例

把"读现有 → 合并 → 写回"整段抽成 `mergeIntoMainCache(mainCache, key, newResults, ttl, isFinal,
keyword, pluginName)`，闭包只剩"空结果短路 + 转调"。抽的时候踩了两个坑，都记下来：

- 闭包所在函数的 `mainCache` 是**参数**而非常量/全局，所以抽出的函数必须同参数名接收，否则函数体
  里所有 `mainCache.` 全部 undefined（同名接收后函数体一行不改）；
- 我先把加锁行补了一遍，而原函数体里**本来就有**那句——重复声明同一变量。抽取脚本必须确认
  "要搬的区间里已经包含哪些行"，不能凭印象补。

### 用例与对照实验

`TestMergeIntoMainCacheConcurrentKeepsEveryPluginResult`：16 个 goroutine 用同一个 key 并发调用
`mergeIntoMainCache`（每个带一个唯一 ID），跑完读回，断言 **16 条一条不少**。

`TestUnlockedMergeLosesResultsControl`：把同一段逻辑**去掉互斥**再跑一遍（即修复前的行为），
在读与写之间插 2ms 放大竞争窗口。结果——**16 个插件只留下 1 条结果**。

这条对照实验的意义不只是"探针没瞎"：它**量化了修复前这个缺陷的实际后果**（16 个插件的结果只剩
1 个，且完全静默），比"理论上会覆盖"强得多。若哪天有人把互斥去掉，这两条用例会同时报警。

### 至此目标项全部完成

重试迁移（含范围更正）、吞错处置（含范围更正）、主缓存并发合并端到端用例，三项都有回归
（49 个包）、对照实验、文档记录、提交与 tag。

## 四十九、重试迁移第十批：AbortError 解锁的 3 处（erxiaopan / panta / yuhuage）

§四十二 把 4 处归为"受限于组件能力"，本轮补齐能力后把其中 3 处迁完（panlian 已在 §四十二 迁完）。

### erxiaopan：三个特点都要保住

可取消等待（`select ctx.Done + time.After`，所以等待留在闭包内、组件侧 `DelayFunc` 返回 0）、
**线性**退避（第 k 次等待 k × retryBackoff）、以及两类**不该重试**的错误用 `util.Abort` 表达：
4xx（429 除外，目标明确拒绝）与"页面超过大小限制"（再取还是超限）。后两类原先靠 `break`/`return`
提前跳出，收敛后必须显式标记，否则会被白白重试。

代码里那句注释"站点域名走 share-dns 轮换（TTL 1 秒），解析失败是常态"是原作者的判断，保留。

### panta：指数退避 + 一个必须保住的既有契约

退避是 `min(backoffBase × 2^(k-1), maxBackoff)`，用 `DelayFunc` 显式表达（不依赖组件默认，
也避免引入抖动）；每次尝试都 `recordResponseTime`，不能因为收敛而丢掉。

**契约点是**：重试用尽时若手上有一个 5xx 响应，就把**该响应连同 nil error**交给调用方（由调用方
按状态码处理），而不是变成 error——原先的循环正是这样返回的。这看起来"不干净"，但它是对外行为，
不能借重构之名改掉。

### yuhuage：线性秒级退避 + "重试用尽返回 nil"是原实现的选择

退避是 `(k) 秒`（原 `time.Sleep(time.Duration(retry+1) * time.Second)`，最后一次不再等待），
用 `DelayFunc` 精确对齐。**重试用尽后直接返回 nil 而不报错**——详情页拿不到就当作没有链接，
由上层决定。这是原实现的选择而非疏忽，所以组件返回的错误在这里被有意丢弃，并在注释里写明原因。

### 这轮又踩了同一个坑（第 4 次）

编辑脚本再次漏收"区间终点之后的那个 `}`"（panta 之后写 yuhuage 时）。四次都靠编译器定位，
这次终于把它固定成强制检查并写进流程：**替换区间终点之后，第一个非空行若是 `}`，且新块已自带
收尾括号，就必须一并吃掉**。这条已足以说明：这类脚本的失败模式是稳定的，可以也应该被程序化拦住。

回归：49 个包通过、`go vet` 零告警。剩余 `for attempt/retry` 形态循环 6 处，均为 §四十二 已定性的
非通用重试（gying、miosou ×2、pansearch、ting77）与域内特例。

## 五十、解压炸弹防护：上游封顶只挡住第一层

### 先纠正一个数字：解压路径整批没被扫到

§二十八 记"上游响应体读取封顶（72 处已修）"，但那一轮只用**一种写法**去扫：`io.ReadAll(resp.Body)`。
解压路径把 reader **换成了变量**——

```go
reader = gzReader
...
io.ReadAll(reader)                 // libvio
goquery.NewDocumentFromReader(reader)  // qupanshe / xiaozhang / leijing
```

于是 **8 处 gzip 解压站点全部漏掉**（libvio、qupanshe ×2、xiaozhang ×2、leijing、xb6v、
util/compression.go），加上 `check_service` 的 gzip/zlib 两处，共 10 处。这与 §四十 更正 zlxapp
时踩的是同一个病根：**用单一文本模式给代码分类，会漏掉等价写法**。所以本轮的守卫按文件判断
（见下），不再依赖某一种写法。

危害不是假想：压缩数据可以很小而解压后极大——实测 **16 MiB 的零字节压成 gzip 只有 16332 字节
（≈1027:1）**，而解压后的内容还要整体物化进内存再交给解析器。

### 两道约束，按数据来源区分

1. **绝对上限**（`MaxDecompressedBytes = 64 MiB`）：适用于所有解压路径。
2. **压缩比上限**（`MaxDecompressRatio = 100`）：只用于**不可信上游**路径。`minDecompressAllowance
   = 400 KiB` 是小输入的保护下限——压缩前只有几 KB 时比值会剧烈波动，只看比值会误伤正常响应。

**缓存路径刻意不套压缩比**：`util.DecompressData` 解压的是自写缓存，缓存里高度重复的 JSON
合法地超过 100:1，按比值拦会误伤真实数据。**压缩比是针对不可信输入的，不是通用卫生措施**。

参数依据说明：这两组阈值的取法来自我了解的成熟实现惯例（同时约束绝对大小与压缩比，比值常用
100:1，如 Apache POI `ZipSecureFile.MIN_INFLATE_RATIO` 默认 0.01）。**本机 web 搜索接口不可用
（API key 失效），因此这些数值未经联网复核**，是按既有知识选取并在本仓量级上验证过的。

### 实现选择：超限要**报错**，不能静默截断

`io.LimitReader` 到达上限后返回 EOF，调用方看到的是"正常结束"——**解析器会把半截数据当完整数据
解析，得到错误结论却毫无察觉**，这是最坏的结果。因此新增 `util.NewCappedReader`：超限时返回
`ErrDecompressionTooLarge`。

这个选择还有一层好处：下游既有的错误分支（`io.ReadAll` 的 err、goquery 的解析错误）自动生效，
**调用点一行都不用改**，8 处解压站点只是一行包装：

```go
reader = util.NewCappedReader(gzReader, util.MaxDecompressedBytes)
```

边界处理与 `ReadAllLimited` 同一套办法：内部允许读到 `limit+1` 字节，多读的那 1 字节用于区分
"正好等于上限"与"超过上限"。用例覆盖三种情形：正好等于上限放行、超 1 字节报错、流式解压被约束。

### 防回归守卫

`main_decompress_guard_test.go`：**按文件**判断——文件里出现 gzip/zlib/flate 解压，就必须同时出现
封顶手段（`NewCappedReader` 或 `ReadAllDecompressed`），否则 FAIL 点名。含植入对照（不封顶的
假文件必须被抓到、封顶后必须放过）。当前全仓 0 命中。

回归：49 个包通过、`go vet` 零告警。

## 五十一、补上最后一个能力缺口：RetryConfig.WaitFunc

§四十 与 §四十二 留下同一件事：`RetryConfig` 的等待是 `time.Sleep`，**不可取消**。zlxapp 与
erxiaopan 原本等的是"可被请求上下文取消"的等待（`time.NewTimer` + `select ctx.Done`），
没有口子时只能把等待留在自己的闭包内、组件侧 `DelayFunc` 返回 0——功能正确但形态特殊。

新增 `RetryConfig.WaitFunc func(wait time.Duration) error`：
- 收到的是**组件算出的等待时长**（所以退避曲线仍由 `DelayFunc`/`BaseDelay` 决定，两者不打架）；
- 返回非 nil 表示**立即停止重试**并把该错误透出，且**不做"重试 N 次后仍失败"的包装**——它是被
  主动中断，不是重试耗尽（用例显式断言这一点）；
- 默认仍是 `time.Sleep`，未设置时行为不变。

两处站点随之归一：`DelayFunc` 表达线性退避（`(attempt+1) × 基准`），`WaitFunc` 表达可取消等待。
**残留的闭包内等待特殊写法：0 处。** §四十 记的那条"取消后仍会把剩余尝试走完"的差异因此消失
——等待被取消时组件直接停止。

用例三条：`WaitFunc` 收到的等待值符合倍率曲线（100ms→200ms）、中断后不再尝试且错误不被包装、
真实取消形态下（等待 5 秒的配置）**0.08 秒返回且只尝试 1 次**。

至此本清单里"记录在案但未做"的项只剩 §二 的两条设计决策（整页 DOM 换分块扫描=暂缓、自动降级
慢频道=不做），两条都是**有据不做**，不是遗漏。

## 五十二、全量验收测试（docker-compose 配置）抓到一个真缺陷

### 验收方法

本机没有 docker，因此按 `docker-compose.yml` 的**同一套环境变量**直接跑二进制：频道与插件列表
**脚本从 compose 文件里抽取**（不手抄，避免转录错误，实际为 110 个 TG 频道 + 71 个插件），
其余变量（CACHE_*、ASYNC_*、PORT）与 compose 完全一致。两处必要偏差已在脚本注释写明：
`CACHE_PATH` 用本机可写目录（容器内是 `/app/cache`）；compose 里 `PROXY` 是注释掉的，本机访问
外网必须走代理，故设为 `PROXY=socks5://127.0.0.1:7897`（同时设 `HTTPS_PROXY`/`HTTP_PROXY`，
分别对应频道路径与插件路径）。

关键词取**豆瓣实时热榜**（`movie.douban.com/j/search_subjects`，经代理可取），四类各一个：
电影《年会不能停！2》、剧集《万物生灵 第七季》、综艺《花儿与少年·丝路季》、动漫《无职转生》。

### 三轮结果

| 关键词 | R1 冷启动 | R2 缓存命中 | R3 forceRefresh |
| --- | --- | --- | --- |
| 年会不能停！2（电影） | 75 条 / 30.1s | **107 条 / 0.14s** | 86 条 / 30.2s |
| 万物生灵 第七季（剧集） | 13 条 / 30.3s | **16 条 / 0.48s** | 13 条 / 30.3s |
| 花儿与少年·丝路季（综艺） | 5 条 / 30.1s | **16 条 / 0.06s** | 5 条 / 30.1s |
| 无职转生（动漫） | 100 条 / 30.2s | **119 条 / 0.19s** | 100 条 / 30.2s |

三个结论：**三轮全部成功**（code=0、非零结果）；**R2 全部是缓存命中且条数严格高于 R1**（后台补齐
生效）；R3 forceRefresh 回到"首轮量级"（86/13/5/100，与 R1 同源）——这三者是自洽的。

同时纠正两个此前基于推测的判断：
- **TG 频道可用**：110 个频道实测 `成功 109/110，失败 0，超时未完成 1`，后台补齐 1/1 后缓存更新为
  完整结果（612 条）。此前我以为该路径在本机不可用。
- **插件侧在异步窗口内只回来少数**：71 个插件常常只有 0~5 个在 4 秒窗口内返回，其余靠后台补齐
  ——日志逐条可见（`本轮零产出（内容需靠后台补齐）`、`慢项`、`失败原因`），没有任何静默归零。

### 抓到的真缺陷：最终写入会把缓存写小

日志证据链：
1. 同一关键词的合并链把缓存累积到 30 条（`原有: 22 + 新增: 8 = 合并后: 30`）；
2. 紧接着 `[主程序] 缓存更新完成: ... | 结果数: 1`；
3. 随后 `✅ [无职转生] 命中缓存 结果数: 1` —— **结果从 30 缩到 1**。

代码坐实：`search_service.go` 的最终写入是 `SetBothLevels(res)` **直接覆盖**，注释还写着"覆盖可能
有问题的异步插件缓存"。但本次请求常常只拿到**部分**结果（71 个插件里 66 个还在后台，而后台插件
是边走边并进主缓存的），于是 `res` 比缓存里已有的更小，覆盖等于把后台已经并进去的结果丢掉。
这与"后写覆盖先写"是同一类问题。

**修复**：最终写入改为**先读现有并合并再写**，并按键互斥（复用与异步插件路径同一把锁）。
抽出 `writeFinalMainCache` 以便测试。

**回归用例 + 对照实验**：`TestWriteFinalMainCacheDoesNotShrink`（后台 8 条 + 本轮 1 条 → 必须
≥9 条）；对照实验 `TestFinalWriteOverwriteShrinksControl` 用修复前的直接覆盖行为，结果只剩 1 条
（8 条后台结果被丢）——证明这个探针测得出来。

**线上复测**：修复后重跑同样三轮，R2 的缓存命中数变为 15/7/11/**28**（修复前无职转生是 1），
且后续合并链显示条目只增不减（`原有: 15 + 新增: 6 = 合并后: 18`）。

### 顺带修掉一个误导性日志

`缓存更新完成 | 结果数: N` 打的是**本次请求自己的条数**，而缓存里实际是合并后的 15 条——这行会
让运维（和我自己）误判缺陷仍在。改为 `本次 N 条 -> 合并后 M 条`。

## 五十三、收尾逻辑归一（第一刀）：两条路径共用写缓存编排

§五十二 的缺陷暴露了重复的真实代价：**修一份漏一份**。插件侧的"最终写入不许把缓存写小"修完了，
TG 侧至今仍是 `enhancedTwoLevelCache.Set(...)` 直接覆盖。所以先动这一步——收益最直接、风险最小。

抽出 `writeSearchCacheByCompleteness(outcome, cacheKey, results, sender)`，两条路径都改调它：
- 判定逻辑（`outcome.cacheTTL` 的全失败不写 / 超时写短 TTL / 其余写正常 TTL）本来就是共享的；
- 重复的是**编排**：序列化 → 写入 → 日志；
- 归一后两边只剩日志标签不同（"主程序"/"频道路径"）。

**行为变化（有意）**：频道路径从"直接覆盖"变成"先合并再写"。理由与插件侧相同——本轮的 results
常比缓存里已有的更少（TG 侧是因为这一轮有频道超时未回），覆盖会把此前累积的结果丢掉且静默。
两条路径各自一套键空间，不会互相污染。顺带把 TG 侧原来的 `Set`（内存即时 + 磁盘异步）换成
`SetBothLevels`（同步两级），与插件侧一致——写本来就在 goroutine 里，不影响响应延迟。

**用例**：`TestWriteSearchCacheByCompletenessMergesForBGothPaths` 对**两个键空间**各验一遍
（已累积 5 条 + 本轮 1 条 → 必须 6 条）。

**这轮两次失败都是用例环境问题，不是被测代码**（如实记录，因为很容易被误判成缺陷）：
1. 第一版没标记任何任务成功 → `cacheTTL` 按"全失败不写"返回不写。那是**正确行为**，是用例前置造错了。
2. 第二版读出 0 条 → 测试配置里 `CachePartialTTLMinutes` 为 0，短 TTL 写入等于立刻过期。
   补上 `CacheTTLMinutes=60 / CachePartialTTLMinutes=3`（与生产默认一致）后通过。

这与 zlxapp 误判、`config.AppConfig` 为 nil 那次是同一类：**测试失败先确认是被测代码还是环境**。

### 剩下的一半

`searchTG`+`backfillTGChannels`（145 行）vs `searchPlugins`+`backfillPlugins`（256 行）的**其余编排**
（任务提交、逐项统计、后台补齐调度）仍是两份。但这部分差异是**设计差异**，不能硬合：
TG 侧用 `TGBackfillEnabled` 开关、插件侧用 `requireYieldTracking` 的整批零产出判定；
`backfillTGChannels` 用"完整结果覆盖缓存"，`backfillPlugins` 带 ext 与并发参数。
要合必须先逐条列出差异再决定哪些是真差异，属于需要产品判断的改动。

## 五十四、给 TG 解析器补一份真实页面夹具（拆 453 行函数的前置）

§五十三 剩下的一半、以及 §三十六 记的 453 行 `ParseSearchResultsWithStatus`，都是纯重构。拆之前
必须先能证明"拆完行为不变"，而这个解析器**全仓只有一个调用方**（`service/search_service.go:695`），
原有测试只有 **2 个**——没有行为基线就动手，等于裸奔。

所以这一步不加任何功能，只补基线：用真实抓取的页面做**行为冻结**。

- 夹具：`util/testdata/tg_channel_shareAliyun_real.html`，真实抓取 `t.me/s/shareAliyun?q=...`，
  152KB、20 个 `tgme_widget_message_wrap` 块。**不用手写合成 HTML**——手写的只能证明"我理解的
  页面结构能被解析"，证明不了真实页面能被解析。
- 用例：`TestParseRealChannelPageFixture` 冻结状态、结果数、每条的前缀/时间/链接类型，
  以及首条的 UniqueID + 标题 + 链接（三个字段一起比对，比"数量对得上"敏感得多）；
  另外冻结域名分布 16×`aliyundrive.com` + 4×`alipan.com`。
- 用例：`TestParseRealChannelPageFixtureUsesChannelInID` 同一页面换频道名，UniqueID 前缀必须
  跟着变——否则不同频道的结果在合并时会撞车。

**关于那个上游现象**：用 `?q=无职转生` 抓到的页面里，消息标题多是别的剧名。查过页面本身：
关键词出现 6 次、被解析出的 20 条是**列表类帖子**（正文里提到该关键词，标题是帖子自己的主题）。
这是上游返回的内容形态，不是解析缺陷。

**这一轮又是"我的假设错、代码对"**：断言里我一开始只认 `aliyundrive.com`，于是 4 条
`alipan.com` 被判成"链接形态异常"——实际解析器把阿里云盘新旧两个域名都正确归为 `aliyun` 类型。
改成同时接受两个域名并冻结分布。这与 §五十三 的两次、zlxapp 那次同属一类：
**测试挂了先确认是被测代码的问题还是断言/环境的问题**，本会话已经出现 5 次，值得当成习惯而不是巧合。

`ParseSearchResultsWithStatus` 的拆分本身仍未做：现在有了基线，可以按"解析单条消息 → 抽链接 →
判定状态"三层拆，且每一步都能用这份夹具比对行为。

## 五十五、拆掉 453 行函数（纯行为保持）

拆之前先补基线（§五十四），拆法是**逐字搬移 + 合并真重复**，不是重写。

结果：453 行的 `ParseSearchResultsWithStatus` 拆成 7 个函数，最大的 66 行：

| 函数 | 行数 | 职责 |
| --- | --- | --- |
| `ParseSearchResultsWithStatus` | 23 | 建文档、遍历消息块、返回状态 |
| `judgeParseStatus` | 12 | 三态判定（原本就已经写清楚了，只是被埋在大函数尾部） |
| `parseTgMessage` | 66 | 解析单条消息：ID/时间/正文/标题/链接/标签/图片 |
| `collectMessageLinks` | 28 | 两个来源（正文与按钮的 `<a>`、正文裸链接）汇到同一套处理 |
| `finalize` | 34 | 按类型补全密码并去重 |
| `extractMessageTags` | 10 | 标签 |
| `extractMessageImages` | 31 | 图片（排除头像） |

**顺手消灭了两处真重复**（这是拆分的实收益，不只是搬家）：
1. "按网盘类型归集"的**六路分支原先写了两遍**（按钮链接一遍、正文裸链接一遍），两处必须保持
   一致却没有任何东西保证。合并为 `linkCandidates.add`——两处语义本来就相同，只有原始 URL 来源
   和密码上下文不同。
2. "按类型补全密码"的**九段循环几乎一样**，合并为 `finalize` 里的一张类型表。类型顺序
   （百度→天翼→UC→123→115→阿里云）与原实现一致；同类型内部多条链接本来就是 map 随机序，未变。

**行为等价性证据**：真实的阿里云盘夹具用例全绿（20 条、首条三字段、域名分布 16/4），合成的六类型
探针输出与拆分前**逐字段相同**（含密码值、链接顺序）。于是把探针固化为
`TestParseAllNetdiskTypesFixture`。

**拆分中发现另一个真问题（未修，单独决策）**：一条消息里多个链接各带自己的提取码时，
**所有链接都拿到第一个码**。实测：合成页面里百度/天翼/UC/123/115 各自的码是 1234/abcd/uc99/p123/p115，
解析结果六个链接的 `Password` 全是 `1234`。

成因在 `ExtractPassword` 的正文回退分支：`strings.Split(content, "提取码")` 后返回第一个合法码，
与链接本身没有关联（天翼/115 用的"访问码"就更不在它的关键词里）。真实页面里 `.Text()` 还会把
`<br>` 折叠成一行，即便上游把每个码写在自己那行也无法就近匹配——而解析器**已经**算出了带换行的
`messageTextWithBreaks`，却只用于标题，没用于密码。

这属于**数据正确性**问题（用错码解不开盘），但修它要定"就近匹配"的规则（多远的码算这条链接的？
找不到时是否回退到现在的整条取首个？），需要产品判断，所以只冻结、不擅改。

## 五十六、提取码按链接就近取值（修数据正确性问题）

§五十五 冻结的那个问题，规则定了：**以链接在带换行正文里的位置为锚，只取其后一小段内的码；
取不到就回退到原来的整条扫描逻辑。** 回退是硬要求——宁可回到旧行为，也不能因为附近没找到就返回空。

新增 `extractCodeNear` / `passwordFor`（`util/parser_util.go`）与 `NearbyPasswordPattern`
（`util/regex_util.go`，长度放宽到 4~6 位再配 `isValidPassword` 校验；原 `PasswordPattern` 写死 4 位，
而真实提取码有 5~6 位的）。`ExtractPassword` 作为公开 API **行为未动**，只被当作兜底调用。

三个关键点都是实测决定的，不是拍脑袋：
- **窗口 60 字符**：短了会漏掉写在下一行的码，长了会吃进别的链接的码；再配合"遇到下一个 `http`
  就截断"的规则，多链接消息不会串码。
- **锚点要多形态**：调用方拿到的 URL 可能已被规范化。实测 123 网盘就是——正文写
  `www.123pan.com`，传进来的是 `123pan.com`，按原样一个都找不到，于是退到旧逻辑、拿错码。
  现在按 完整URL → 去 scheme → 去 www. → 路径末段 依次尝试。
- **窗口末尾按 rune 边界回退**：否则可能切出半个多字节字符。

**验证**（合成布局 + 真实页面，各自说明能证明什么）：
- 对照实验 `TestExtractCodeNearControlOldBehaviorWasFirstMatch`：旧逻辑对六个链接一律返回首个码
  `1234`——证明探针测得出来，不然改断言只是在改数字。
- `TestExtractCodeNearPerLink`：修复后六个链接分别拿到 `1234/abcd/uc99/p123/p115/`（最后一个该行为空）。
- 边界：码写在下一行（`TestExtractCodeNearCrossesNewline`）、两链接相邻不串码
  （`TestExtractCodeNearDoesNotCrossIntoNextLink`）、锚点被规范化
  （`TestExtractCodeNearHandlesNormalizedAnchor`）、长上下文定位不到则返回空
  （`TestExtractCodeNearUnanchoredLongContextReturnsEmpty`）、回退不返回空
  （`TestPasswordForFallsBackInsteadOfEmpty`）、不切半个字（`TestExtractCodeNearDoesNotSplitRune`）。
- 新增第二份真实夹具 `testdata/tg_channel_leoziyuan_with_codes.html`（176KB、20 个消息块、
  502 条链接、19 条带密码，实测值冻结）。

**必须说清的一点**：在抓到的真实页面上，新旧逻辑结果**完全相同**——码多数直接写在 URL 的
`?pwd=` 里，两条路径都走 URL 分支（实测正文形如 `🔗 百度网盘：https://pan.baidu.com/s/...?pwd=g3fa
🔑 提取码：g3fa`）。所以这份真实夹具证明的是"没有退化"，**不是"修复有效"**；修复有效的证据来自
合成布局的对照实验。真实世界里"码只在正文、不在 URL"的多链接帖子是否常见，本会话没有数据，
因此**线上收益未经验证**，只确认了失败模式确实存在。

**保留的残留歧义**（有意，不修）：链接自己没码、但消息里另有别的链接的码时，回退逻辑仍会把那个码
给它。因为"多个链接 + 末尾一个码统管"的写法同样常见，纯就近取值会让这些链接一个码都拿不到——
两种写法在文本上无法区分，只能选"不丢码"的那一侧。

## 五十七、补齐路径也改为合并写（含守卫）+ 一次诚实的 A/B：收益约为零

### 改了什么

§五十六 之前发现两个 backfill 的写回是整块覆盖、且无按键互斥（`search_service.go` 的
`Set` / `SetBothLevels`）。现已统一改为 `writeFinalMainCache`（先与缓存里已有的合并、按键互斥），
日志同时打印"本次 N 条 -> 合并后 M 条"。这是同一类缺陷的第 3 次出现。

**加了文件级守卫** `TestMainCacheWritesGoThroughMergeEntrypoints`：除了 `writeFinalMainCache`、
`mergeIntoMainCache` 与写入管理器的落盘回调，任何地方直接 `Set/SetBothLevels` 主缓存都会失败。
守卫带对照实验：临时把旧覆盖写植回 TG 补齐处，守卫立刻报出 `search_service.go:1467`，恢复后通过。
按文件级拦而不是按调用点，是因为这类问题已经出现三次，靠"记得每一处"不可靠。

### A/B 实测（同一关键词 6 个并发请求 + 等后台补齐后再搜一次）

| 指标 | 修复前（覆盖写） | 修复后（合并写） | 差异 |
| --- | --- | --- | --- |
| 并发 6 次各自返回 | 100 / 100 / 100 / 100 / 100 / 100 条 | 同样 100×6 | **无** |
| 并发请求耗时 | 30.2s | 30.1s | **无** |
| 补齐后再搜一次 | 136 条 / 0.09s | 135 条 / 0.17s | **无**（±1 是上游波动） |
| 补齐写入日志 | `缓存已更新为完整结果（592 条）` | `本次 612 条 -> 合并后 612 条` | 合并**等于无操作** |
| 受控测量（构造"并发方各持不同结果"） | 50 条只剩 13 条，丢 74% | 保住 63 条 | 有差异，但**该场景真实性未被证实** |

**结论必须照实说：这一项在实测里看不到任何用户可见收益。**

原因是日志直接给出的：6 个并发请求搜同一关键词、同一批插件，产出的是**同一批结果**，
所以"覆盖"与"合并"完全等价（合并后 612 = 本次 612）。受控测量里那个 74% 需要"并发写入方各自
持有不同结果集合"，而同一关键词的重复搜索不产生这种情况。上一节把这个 74% 当成预期收益来陈述，
是**过度声称**——它只证明了机制存在，没有证明它在生产里会发生。

真正观测到过的那次丢结果（30 条写成 1 条、丢 97%）是**完整度写入**，属于已经修掉的那一处；
它成立是因为缓存里那 30 条是**后台插件逐步并进去的**，确实与单次请求的快照不同。

### 这次测量反而给出了一个可测量的真实杠杆

同一次 A/B 里：首轮并发请求返回 **100 条**，等后台补齐后同一关键词是 **135~136 条**。
即**首轮少给用户约 26% 的结果**，差额全靠后台补齐补回。这是本次唯一测出的、用户可感知的数量差。

要不要动它取决于取舍：把 `ASYNC_RESPONSE_TIMEOUT`（当前 4 秒）调大能让首轮拿到更多，代价是首轮变慢。
但本次请求耗时稳定在 30.1~30.3 秒，说明首轮延迟由别的环节主导（不是这 4 秒），
**在动这个参数之前需要先测清那 30 秒花在哪里**，否则可能白调甚至变慢。

## 五十八、首轮 30 秒延迟归因：是插件批任务的软截止

### 定位过程

先排除：TG 批任务超时不是它——`TG_CHANNEL_TIMEOUT_SECONDS` 默认 0，回退到单频道 4 秒
（`TGChannelRequestTimeout`），实测频道批在 3.4 秒内结束。也不是 `ASYNC_RESPONSE_TIMEOUT=4`：
那条异步窗口管不到这条同步调用路径。真正的阻塞点是 `search_service.go` 的插件批任务：

	batchTimeout := config.AppConfig.PluginBatchTimeout   // PLUGIN_BATCH_TIMEOUT_SECONDS
	if batchTimeout <= 0 { batchTimeout = config.AppConfig.PluginTimeout }  // 默认 30 秒
	results := pool.ExecuteBatchWithTimeout(tasks, concurrency, batchTimeout)

请求在 `wg.Wait()` 处一直等这个批任务（`search_service.go` 的主流程），所以批截止就是首轮延迟上限。

### 三关键词配对实测（每次都是全新缓存，同一二进制，仅改环境变量）

| 关键词 | 批截止 30s（默认） | 批截止 8s | 结果差 | 延迟差 |
| --- | --- | --- | --- | --- |
| 凡人修仙传 | 821 条 / 30.20s | 821 条 / 8.19s | **0** | −22.0s |
| 仙逆 | 452 条 / 30.20s | 455 条 / 8.20s | +3 | −22.0s |
| 遮天 | 1851 条 / 30.14s | 1877 条 / 8.16s | **+26** | −22.0s |

只设 `PLUGIN_BATCH_TIMEOUT_SECONDS=8`、把 `PLUGIN_TIMEOUT` 留在 30 秒，结果同样成立
（8.19s / 821、8.19s / 457、8.16s / 1877）。**两个变量是两件事**：前者是请求路径的等待上限，
后者是插件后台 HTTP 客户端超时；只降前者才既省时间又不砍后台抓取。

顺带修正上一节的一处错误结论：当时单次对比得到"8 秒档 526 条 vs 30 秒档 821 条"，
据此以为砍掉了三成结果。配对复测（同一关键词、先后两次、各自全新缓存）显示差异来自上游波动，
不是批截止——**单次对比不可用作结论**，这条要记住。

超时的插件恰好是本身就卡住的那批，它们的结果仍会经后台补齐与插件自身的后台刷新进入缓存，
这就是"零损失"的原因。

### 已落地

`docker-compose.yml` 增加 `PLUGIN_BATCH_TIMEOUT_SECONDS=8`，注释里写明实测数据与两个变量的区别。
可逆：删掉这一行即回到 30 秒。证据范围仅这三个关键词（外加前面的无职转生），不是全量关键词普查。

## 五十九、主缓存存在两个 TTL 在互相覆盖（待决策，未改）

排查上一节验收里"同间隔却一个命中一个未命中"时发现的：同一个主缓存键有多个写入方，
**TTL 不一致**。

实测写入序列（`凡人修仙传`，全新缓存，插件侧主缓存键 dd445083...）：

	🔄 [ting77:凡人修仙传] 更新缓存| 原有: 54 + 新增: 4 = 合并后: 58 | TTL: 60分钟
	🔄 [lou1:凡人修仙传] 更新缓存| 原有: 58 + 新增: 6 = 合并后: 64 | TTL: 60分钟
	[主程序] 缓存更新完成: dd445083... | 本次 2 条 -> 合并后 64 条 | TTL: 3分钟

- 插件合并路径（异步插件边跑边并）写 **60 分钟**（取 `ASYNC_CACHE_TTL_HOURS`）
- 完整度写入写 **3 分钟**（部分结果，`CACHE_PARTIAL_TTL_MINUTES`）

条目最终活多久、以及内容是哪一版，取决于**谁最后写**：完整度写最后落地 → 3 分钟后过期，
下次同关键词全量重搜（实测第二次 30.20s，正是这条）；插件合并最后落地 → 条目活满 1 小时，
命中但可能是**较旧的快照**（验收里那次命中读到 50 条，而最后一次完整度写入记的是 54 条）。

已排除的：不是淘汰（日志无淘汰记录）、不是"间隔没超 3 分钟"（三个间隔 3:23/3:52/3:53 全部超过）。

这与 `Get` 里的注释直接冲突——那里明写"不能一律用 CacheTTLMinutes，否则短 TTL 分流会被整个绕过"，
而插件合并路径此刻正在做这件事。**修法涉及产品取舍**（部分结果到底该存活多久），
需要先定策略再动，因此本节只记录证据、不改代码。

顺带把主缓存的写入日志补全：插件合并日志加上 TTL，两个"按字节落盘"的回调
（全局写入管理器、缓存写入集成）加上 key/TTL/字节数，并按 `ASYNC_LOG_ENABLED` 收口避免刷屏。
没有这些字段，上面这条现象无从追溯。

## 六十、删掉"有超时写短 TTL"这一档（我上一节引入的分档，门槛判错了对象）

### 这一档原本干什么

`batchSearchOutcome.cacheTTL` 有三档：

	succeeded == 0        -> 不写缓存（全部失败）
	requireYield && yielded == 0 -> 不写缓存（整批零产出）
	timedOut() > 0        -> 写 partialTTL（3 分钟）      ← 本节删掉的就是这一条
	否则                  -> 写 fullTTL（60 分钟）

它只影响一件事：**下次同关键词搜索是命中缓存还是重新全量搜**。

当初加它的理由（`9a3c66d` 的提交信息原文）："一次抖动导致残缺结果污染整个缓存周期"——
若本轮只拿到很少结果却不设短 TTL，这条残缺结果会躺满 60 分钟，期间所有同关键词搜索都命中它、
不再重搜，用户一小时都看到残缺结果。

### 为什么它判错了对象

门槛写的是"有任意一项超时"，但插件路径下**超时是常态**：异步窗口只有 4 秒，全量配置实测每轮
都是 `成功 0~4/71、超时未完成 66~71`。于是分档 100% 触发，插件侧主缓存永远只活 3 分钟，
而同一关键词的 TG 侧因为频道没超时活 60 分钟；再叠加插件合并路径的 1 小时 TTL，
条目存活时间与内容版本取决于谁最后写。**同一个关键词，一半缓存 3 分钟、一半缓存 1 小时。**

### 利与弊（都是实测）

**利：过期重搜能多拿多少？**

| 关键词 | 未过期命中的累积结果 | 过期后重搜 | 重搜增益 |
| --- | --- | --- | --- |
| 凡人修仙传 | 876 条（比首轮 +55） | 844 条 | **−32** |
| 仙逆 | 过期，重搜 452 | 457 条 | +5（1.1%） |
| 遮天 | 1923 条（比首轮 +72） | 1851 条 | **−72** |

结果变多靠的是**缓存不死、后台合并往里加**（+55、+72），不是过期重搜。这一档买到的增益约为零。

**弊：**命中 0.16~0.20 秒 vs 过期重搜 30.2 秒（8 秒批截止下为 8.25 秒），差一个到两个数量级，
且用户无法预知。

### 改动与复测

删掉第三档，保留前两条"不写缓存"（它们才是防抖动的正主：全部失败、整批零产出）。
`CACHE_PARTIAL_TTL_MINUTES` 与环境变量保留但标注废弃，仅为兼容既有部署。

复测（凡人修仙传，冷缓存起步，中间等 240 秒——远超原来的 3 分钟过期点）：

| 时刻 | 结果 | 用时 |
| --- | --- | --- |
| 第 1 次（冷） | 821 条 | 8.25s |
| 第 2 次（240 秒后） | **1005 条** | **0.19s** |

改动前这里必然是插件侧重搜；改动后命中存活，且条数比首轮多 184 条（+22%）。
日志侧也能看到写入 TTL 已统一为 60 分钟（`[频道路径] ... | TTL: 60分钟`），
不再出现同一关键词两侧不同寿命。

**尚未做的**：内存/磁盘两级视图不一致（非最终合并只写内存、最终写入双写，读取固定先看内存，
所以内存里的 50 条会遮住磁盘上的 54 条）。这是独立于 TTL 策略的一处小修，未动。

## 六十一、撤回：把 8 秒写进部署配置是越界（默认值属于运维，不属于我）

### 撤回的内容

`docker-compose.yml` 里我加的 `PLUGIN_BATCH_TIMEOUT_SECONDS=8` 已删除，
回到项目默认：该变量为 0 时沿用 `PLUGIN_TIMEOUT`（30 秒）。**代码未动**——8 秒从来只是环境变量的值，
Go 里没有这个常量。

### 撤回的理由

1. **默认值属于项目与运维，不属于我。** 我实测的是"这三个关键词下 8 秒不掉结果"，
   而不是"8 秒对所有部署都更好"。把它写成部署默认，等于把未验证的范围推给所有使用者。
2. **超时是运维可调项**，而且 `api/` 层没有任何请求级超时参数（已核查），
   要调只能通过环境变量——正因为如此，默认值更该保持项目原样，由运维按自己的链路决定。
3. 更根本的一点：**"让请求陪着等 30 秒"本身不是缺陷**。30 秒同时是"插件可工作多久"与
   "用户愿意等多久"，把两者分开是设计选择，不是唯一正确答案。我之前的表述把它说成了缺陷。

### 保留的部分（与撤回无关，各自独立）

- **归因结论保留**：首轮固定 30.1~30.3 秒来自插件批任务的软截止（`search_service.go` 的
  `batchTimeout` 回退到 `PluginTimeout`），不是 TG 批任务（4 秒）、也不是 `ASYNC_RESPONSE_TIMEOUT`。
  这是定位，不是改动。
- **实测数据留档**：需要更短首轮的人可按自己的链路设 `PLUGIN_BATCH_TIMEOUT_SECONDS`
  （30 秒 vs 8 秒，凡人修仙传/仙逆/遮天为 821·8.2s / 455·8.2s / 1877·8.2s，30 秒档为 821·30.2s /
  452·30.2s / 1851·30.1s）。只测过这两个点，中间值未扫。
- 同批的另外两项改动与本条无关，继续保留：删除我引入的"有超时写短 TTL"分档（实测命中 0.19 秒、
  条数 +22%）、以及主缓存写入日志补全。

### 教训

凡是想改变**别人部署的默认行为**，只能提出数据、不能直接落进对方的配置文件。
代码内的默认值同理：加变量可以，替对方定值不行。

## 六十二、全局插件超时 30 秒 -> 10 秒（按项目所有者指示实施），以及逐插件补超时的结论

### 先纠正我自己的两个错误判断

1. 我先说"有插件挂住 30 秒"，**被实测推翻**：单插件逐个跑（`src=plugin&plugins=<名>`，
   全量配置那 71 个启用插件，关键词"凡人修仙传"）最慢 **4.04 秒**，且那 4.04 秒正是 4 秒异步窗口，
   超过 8 秒的插件 **0 个**。
2. 我还一度按 `DefaultTimeout` 这个名字数"漏设超时的插件"，报出 25 个，**数错了**：
   插件用别的名字（如 panlian 的 `RequestTimeout`）也算了，按任意超时常量统计是
   **111 个插件目录里 71 个有、40 个没有**。

### 隔离实测：30 秒到底花在哪

| 请求 | 结果 | 用时 |
| --- | --- | --- |
| 仅 TG 频道（`src=tg`） | 821 条 | **3.46s** |
| 仅 71 插件（`src=plugin`） | 13 条 | **30.00s** |
| 全部（`src=all`，缓存命中） | 834 条 | 0.25s |

用户等待由**插件批任务等满软截止**决定，与 TG 路径无关。

**但机制仍未定死**：单插件最慢 4.04 秒，说明变慢是**并发争抢**（71 个插件共用一个出口）
造成的，而不是某个插件本身慢。重试日志只有 9 行，不足以解释 30 秒。
**这一点必须记下来：那份单插件扫描测不出并发争抢，不能用来给单个插件定超时值。**

### 实施：`PLUGIN_TIMEOUT` 默认 30 秒 -> 10 秒

这个值一身兼三职：没自定义超时的插件的后台 HTTP 客户端上限、插件批任务软截止的回退值、
后台补齐批截止的回退值。改默认值即三处一起生效；代码内两处 30 秒兜底一并改为 10 秒。
需要更长等待的部署用 `PLUGIN_TIMEOUT` 覆盖。

实测（全量配置、各自全新缓存）：

| 关键词 | 30 秒档 | 10 秒档 | 条数差 | 时间差 |
| --- | --- | --- | --- | --- |
| 凡人修仙传 | 821 条 / 30.20s | 824 条 / 10.22s | +3 | −20.0s |
| 仙逆 | 452 条 / 30.20s | 463 条 / 10.23s | +11 | −20.0s |
| 遮天 | 1851 条 / 30.14s | 1855 条 / 10.14s | +4 | −20.0s |

### 对"给 40 个缺少超时的插件逐个补上"的结论

**光补逐个插件的超时，不会缩短这个等待。** 因为：等待由批任务软截止决定，而逐插件的上限
要么等于它现在继承的值、要么更紧——两者都只是把"某个任务何时被放弃"提前或推后，
不改"用户等多久"这个由批任务软截止决定的上限。要让首轮变快，改的是批任务软截止（本次已做）。

补逐个插件超时仍有价值，但价值在**可读性与可逐个调**（不再有插件默默继承全局值），
不在于延迟。而且**取值必须来自并发场景的数据**（在全量搜索中记录每个任务的耗时），
不能用单插件扫描的数据——那是错的仪器。这项工作未做。

## 六十三、2x2 实测：把全局超时降到 10 秒、把后台工作池砍到 4，后台补齐都没丢数据

### 起因

担心 `PLUGIN_TIMEOUT=10` 顺带把**插件后台 HTTP 客户端超时**与**后台补齐批截止**一起压到 10 秒，
于是后台补齐可能丢结果；进一步担心：并发/线程池被用户调小时，排队变长，10 秒更不够用，
是不是就该配更长的全局超时。

### 实测（全量配置，关键词"凡人修仙传"，各自冷缓存；首轮后等 85 秒再搜一次，
### 此时命中缓存，读到的就是"已含后台补齐"的落定值）

| 配置 | 首轮 | 85 秒后落定 |
| --- | --- | --- |
| `PLUGIN_TIMEOUT=30` / 后台工作池 20 | 821 条 / 30.20s | **901 条** |
| `PLUGIN_TIMEOUT=10` / 后台工作池 20 | 821 条 / 10.25s | **902 条** |
| `PLUGIN_TIMEOUT=10` / 后台工作池 4 | 824 条 / 10.23s | **902 条** |
| `PLUGIN_TIMEOUT=30` / 后台工作池 4 | 821 条 / 30.24s | **899 条** |

四格落定值 899~902，**差 3 条（0.3%）**，即噪声。

结论：
- 全局超时 30 -> 10 **没有丢后台补齐的数据**（901 对 902）。
- 后台工作池从 20 砍到 4 **也没有**（902 对 899，甚至略高）。
- 首轮延迟仍然是 30.2s 对 10.2s。

机制上说得通：批截止只限定**等待**，不终止插件自己的后台任务；后台任务照旧跑完并合并进缓存，
所以等待被砍短不等于结果被丢弃。

### 这份测量的适用边界（必须说清）

- 只测了工作池 20 与 4 两档，**没测 1~3**。极端小池（71 个插件挤 1~2 个槽）没数据。
- 只测了一个关键词。四个格子的差值本身在噪声范围内，不能用它去推断小差异。
- 因此"并发调小就需要更长全局超时"这一假设**目前没有证据支持**，但也没被极端值证伪。

### 顺带记一个测试脚手架的坑

`pkill -f pansou-acceptance` 会把**调用方自己的 shell** 一起杀掉——因为整段命令文本里含有这个字符串，
`-f` 是匹配完整命令行。本次因此有两个阶段被 SIGTERM 中断、两格数据缺失。
改用 `pkill -x pansou-acceptance`（按可执行文件名精确匹配），并在起服务后轮询健康检查
（不要固定 sleep 12 秒，负载高时服务没就绪会连不上）。
